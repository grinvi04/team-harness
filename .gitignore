# macOS
.DS_Store

# 목표·마일스톤 로컬 대시보드 — 정본은 GitHub Milestone/Issue (/goal·/milestone은 커밋 안 함)
# team-harness도 자기 표준(templates/gitignore.snippet)을 dogfood — decisions K3
docs/goals/
docs/milestones/

# 시크릿 — 공개 저장소에 민감 파일이 staging되기 전 차단
.env
.env.*
!.env.example
*.key
*.pem
