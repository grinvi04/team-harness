# 의사결정 기록 (Decision Log)

확정된 기술·프로세스 결정의 **단일 출처**. "무엇이 언제 확정됐고, 어느 문서가 정본인가"를 한 곳에서 답한다.

> 존재 이유: 결정이 문서 곳곳의 "(확정)" 표기로 흩어져 있으면, 먼저 쓰인 문서에 후속 결정이
> 역반영되지 않아 문서 간 모순이 생긴다 (2026-06 정합성 검토에서 동일 패턴 3건 확인).

## 규약

- 새 결정 확정 시: **이 표에 행 추가 + 영향받는 문서 갱신을 같은 PR에서** 처리한다
- 결정 변경 시: 행을 지우지 않고 상태를 `대체됨(→新행)`으로 바꾼다 — 이력 보존
- "검토 중"인 것은 여기 적지 않는다 — 확정만 기록 (후보 비교는 stack-guide.md 영역)

## 결정 목록

| 결정 | 시점 | 정본 문서 | 영향 문서 |
|---|---|---|---|
| **⚠️ #79에서 솔로 표준(승인요건 0)으로 대체됨** — git-flow + main/develop branch protection (원안: PR·승인 1+·스레드 resolve 서버 강제 / 현행: 승인 0·enforce_admins=on·CI-gate, 팀 모드는 #105 opt-in) | 2026-06 | onboarding.md | code-review.md, AGENTS.md, guard.sh |
| 거버넌스 배포 = 플러그인 버전 배포 (파일 복사·동기화 스크립트 금지) | 2026-06 | README.md | harness-maintenance.md |
| 규약 단일 출처 = AGENTS.md (도구별 전용 지침은 각 도구 파일에만) | 2026-06 | templates/AGENTS.md | CLAUDE.md, ai-collaboration.md |
| 커밋 = Conventional Commits (타입 영어 + 본문 한국어) | 2026-06 | code-review.md | operations.md(CHANGELOG) |
| DB = PostgreSQL (NoSQL은 보조 용도만) | 2026-06 | stack-guide.md | db-standards.md |
| 인증 = Keycloak OIDC, 자체 구현 금지 / 인가 = RBAC 권한코드 + 데이터 스코프 | 2026-06 | auth-standards.md | architecture-infra.md, stack-guide.md |
| API 응답 = 공통 Envelope (code·message·data), offset 페이지네이션 기본 | 2026-06 | api-standards.md | — |
| 마이그레이션 = forward-only (down 스크립트 금지), BIGINT PK + 채번 | 2026-06 | db-standards.md | skills/release-check/skill.md |
| 아키텍처 = 모듈러 모놀리스 → 미니서비스, 1차 경계 = 도메인 모듈 | 2026-06 | architecture-infra.md | clean-architecture.md |
| 모듈 내부 계층 = adapter → application → domain (interface/infrastructure 분리 안 함) | 2026-06 | clean-architecture.md | stack-guide.md |
| 배포 = EC2 단계 GitHub Actions push형 → EKS 단계 Argo CD pull형 | 2026-06 | architecture-infra.md | stack-guide.md |
| 시크릿 = AWS Secrets Manager/SSM (.env를 서버에 두지 않음) | 2026-06 | stack-guide.md | auth-standards.md, gitignore.snippet |
| 모델 티어링 = Haiku(단순)·Sonnet(빌드·메인 기본 ⚠️Opus로 대체 — 아래 "현행화" 행)·Opus(검증·설계·리서치), 메인 Haiku 불가 | 2026-06-21 | model-tiering.md | 커맨드 subagent `model:` 지정 |
| 마일스톤 레이어 추가 = `/milestone` 스킬 — GitHub Milestone 단일 출처, `/plan` 위에서 마일스톤·진행률 관리. 내장 `/goal`(세션 stopping condition)과 이름 충돌 회피로 rename | 2026-06-23 | skills/milestone/skill.md | README.md |
| `/plan` 스킬에 Claude Code plan mode 흡수 — Phase 0 진입 시 EnterPlanMode, Phase 5 승인 시 ExitPlanMode로 read-only 강제를 prose 규칙에서 메커니즘으로 승격 | 2026-06-23 | skills/plan/skill.md | — |
| 동기 조건-루프 = `/loop` 스킬 — exit 0 달성까지 즉시 반복, max·stuck·checkpoint 안전 장치. 내장 `/loop`(ScheduleWakeup 비동기 예약)와 별개 | 2026-06-23 | skills/loop/skill.md | README.md |
| 도메인·모듈별 번호(또는 브랜치) 규약은 구조적 out-of-order를 만든다 — 새 저버전 마이그레이션이 기존 적용분보다 낮음. 모듈별 독립이면 도구의 out-of-order 허용을 켠다(없으면 기존·운영 DB validate 실패, CI는 빈 DB라 통과) | 2026-06-28 | db-standards.md | flyway.md |
| 소프트삭제 필터는 모든 대상 엔티티/모델에 실제 적용되는지 삭제 후 제외 테스트로 검증 — ORM에 따라 상위 타입 선언이 하위 타입에 미상속될 수 있음 | 2026-06-28 | db-standards.md | java.md |
| 클라이언트 입력 오류(역직렬화·타입변환·검증 위반)는 4xx 매핑, 5xx 흡수 금지 / 낙관적 잠금 update 응답은 flush 후 매핑(stale version 방지) | 2026-06-28 | api-standards.md | java.md |
| 인증·데이터 의존 기능은 실 IdP 인증 + 실 백엔드 데이터 통합 e2e를 별도 게이트(env 플래그)로 — 더미세션 렌더 스모크로 갈음 금지 | 2026-06-28 | code-review.md | — |
| 위 교훈들을 prose→메커니즘으로 승격: 마이그레이션 안전성 정적 게이트(`scripts/check-migration-safety.mjs`+CI `migration-safety`), 게이트 스킬 체크 강화(release-check·pr-review-gate), 신규 프로젝트 템플릿 기본값(out-of-order: true·integration-e2e 스캐폴드) | 2026-06-28 | scripts/check-migration-safety.mjs | release-check/skill.md, pr-review-gate/skill.md, flyway.md, templates/ci/migration-safety.yml, templates/ci/integration-e2e.yml |
| 게이트 결함 수정(v0.8.0): migration-safety의 on.paths 필터 제거(항상 실행+스크립트 self-skip, required check가 영구 pending으로 안 막히게) · 정적 게이트는 Flyway 전용임을 정직하게 스코프(Alembic 다중 head는 별도 CI 점검) · integration-e2e를 new-repo.sh가 env-gated required로 자동 배선 · 보안 핵심 로직 mock-only 테스트 점검 + 배포 env-key 대조 + e2e 격리 + CSV formula injection 갭 보강 | 2026-06-29 | scripts/check-migration-safety.mjs | templates/ci/migration-safety.yml, scripts/new-repo.sh, flyway.md, alembic.md, security-reviewer.md, release-check/skill.md, pr-review-gate/SKILL.md, typescript.md, java.md, db-standards.md, api-standards.md, operations.md |
| 모듈 내부 계층 표기 정합화: java.md가 `interfaces`/`infrastructure` 계층을 기술해 clean-architecture.md(adapter→application→domain, 분리 안 함)와 모순 → java.md를 decisions 기준으로 수정(역반영 누락) | 2026-06-29 | clean-architecture.md | java.md |
| 기존 repo 드리프트 점검 메커니즘(`check-repo-sync.mjs`) 추가 — 신규=new-repo.sh와 대칭. templates/가 기존 repo에 자동 전파 안 돼 표준 게이트 누락 드리프트가 필연이라, 스택 감지 후 필수 harness 자산(test-guard·commitlint·secret-scan·migration-safety·룰) sync를 점검(MISSING=exit 1). 오탐 회피: ci-gate 존재만·게이트는 내용 sentinel·무관 스택 스킵. v0.9.0 | 2026-06-29 | scripts/check-repo-sync.mjs | docs/harness-maintenance.md, scripts/new-repo.sh, templates/ci/repo-sync.yml, templates/ci/ci-gate.yml |
| check-repo-sync를 플러그인으로 이전 + `/repo-sync` 스킬 — 설치 후 어디서나 호출, 루트 shim으로 기존 경로 호환. v0.10.0 | 2026-06-29 | plugins/harness-guard/scripts/check-repo-sync.mjs | plugins/harness-guard/skills/repo-sync/skill.md, scripts/check-repo-sync.mjs(shim), docs/harness-maintenance.md, plugin.json, README.md |
| 서브에이전트 타입↔티어 정합(v0.10.1): read-only/판단없는 작업은 `Explore`(최소권한·Haiku), 파일수정·빌드는 `general-purpose`(Sonnet). release Phase5 헬스체크 `general-purpose`→`Explore`(trivial read-only) · release-check Agent C(마이그레이션 안전감사=판단형) `model: haiku`→`sonnet`로 정정(model-tiering 표의 "마이그레이션=Sonnet"과 skill 불일치 해소) · milestone/goal S1은 파일 수정이라 general-purpose 유지 | 2026-06-30 | model-tiering.md | skills/release/skill.md, skills/release-check/skill.md, plugin.json |
| guard 차단 이력 로깅(v0.10.2): guard.sh의 모든 deny를 `deny()` 단일 경로로 중앙화(드리프트 방지) — 차단 시 `~/.claude/hooks/guard-block.log`에 `session_id·cwd·사유·명령`(한 줄 정제) 기록. 사용자 메시지·exit 2 동작 불변, 로깅만 추가. 동기: 멀티세션 동시 작업 시 어느 세션이 위반을 시도했는지 사후 감사 불가 사각 해소(서브에이전트 모델 로그 session_id 강화와 대칭). guard-test 16/16 회귀 없음 | 2026-06-30 | plugins/harness-guard/scripts/guard.sh | README.md, plugin.json |
| 의도 라우터(v0.11.0): UserPromptSubmit 훅 `route-intent.mjs`가 매 프롬프트마다 git/PR 상태→하네스 단계를 판정해 "진행해/해줘" 류 캐주얼 지시에 다음 단계 스킬 호출을 컨텍스트 주입. 동기: AI(나)의 *반사적 맨손 gh/git* 실패모드 교정 — 메모리·약속은 세션/PC 넘어 안 가므로 라이브 상태 기반 주입으로 대체. 관점: "막기(prohibition)는 텍스트 grep이라 난독화로 무한 우회→과속방지턱일 뿐"이라 적대적 봉쇄 대신 **자동화(1)+구조(3)** 로 선의의 반사를 올바른 길에 태움. 정직한 한계: 강한 안내지 un-bypassable 아님(난독화 봉쇄는 별도 경계=크레덴셜격리/서버사이드 필요, 비범위). read-only·fail-open·오버트리거 0 설계. TDD(route-intent-test 13케이스), ci-gate 배선 | 2026-06-30 | plugins/harness-guard/scripts/route-intent.mjs | hooks.json, tests/route-intent-test.sh, ci-gate.yml, README.md, plugin.json |
| 의도 라우터 키워드 확장(증분2: plan·feature-modify·hotfix·release·qa·repo-sync·loop·milestone) 시도→**폐기**. 이유: **키워드 기반 라우팅은 근본적으로 오버트리거**한다 — 이 스킬들의 자연어 지시가 일상 코딩어와 substring 충돌(`/code-review` 2회+라이브 실증: "for 루프 고쳐줘"→/loop, "장애인 접근성 고쳐줘"→/hotfix, "DB 동기화 로직 만들어줘"→/repo-sync, task-notification "핫픽스 해줘" 텍스트가 hotfix 오주입). **원칙: 상태 기반 라우팅만 고정밀**(openPR·feature+커밋·spec 등 상태신호는 모호0), 키워드는 불가. **거짓 주입 > 무주입**(틀린 "/X 호출하라"는 AI를 적극 오도)이므로 라우터는 정밀도 우선·재현율 양보. 키워드 좁히기는 두더지잡기(altitude). **route-intent.mjs 확장 시 필독**: 상태로 disambiguate 가능한 것만, 또는 명백 비충돌 트리거('핫픽스'/'hotfix' 같은 하네스 전용어)+상태조건으로 1개씩. 회피할 정확성 버그: DEV/MODIFY `branch==='develop'` 하드코딩(main 기반 repo서 미발동→origin/HEAD 동적), prompt 길이/형태 가드 부재 | 2026-06-30 | plugins/harness-guard/scripts/route-intent.mjs | (증분2 미머지) |
| 프로젝트 가시성·라이선스 정책: **개인(솔로) 코드 프로젝트 = public repo + MIT 라이선스**. 사유: GitHub Free 플랜은 **public repo에서만 branch protection 무료**(계층0 강제의 전제) — private는 Pro 필요. team-harness가 강조하는 "강제는 서버에"가 솔로에서도 성립하려면 public이어야 함. **예외: team-harness 자체 = private**(⚠️ **#73에서 public 전환으로 대체됨** — 이 예외 무효)(거버넌스 메타 repo, 표준 노출 최소화) — 단 그 결과 자체 branch protection은 못 걸려 솔로 머지는 `/solo-merge` + 사람 승인에 의존. 프로필/문서 전용 repo도 예외. **신규 코드 프로젝트는 `~/project` 추가 시 team-harness 플러그인·경량 CLAUDE.md·공통 표준 보호·secret-scan 준수 필수.** | 2026-06-30 | onboarding.md | new-repo.sh, readme-standards.md(MIT) |

| 프로젝트 dev 권한 = 커밋 settings.json 단일출처(v0.12.0): gitignore된 settings.local.json(레거시·옛 harness-init.sh 생성)이 아니라 **커밋된 `.claude/settings.json`**에 dev 권한을 둔다 — travels(새 PC)·공유·repo가 이미 스택 노출하므로 커밋해도 무방(비공개는 repo private). 전역 `~/.claude/settings.json`은 범용 개인용이라 dev 권한 안 넣음. 구현: `templates/settings.json`에 공통 베이스라인(git·기본도구+deny sudo/ssh/.env), 스택별은 `templates/permissions/<스택>.json` fragment를 `scripts/merge-permissions.mjs`(무의존·dedup·테스트 6케이스)가 new-repo.sh에서 병합(typescript→node, prisma·java·python·alembic, docker는 backend 스택). **settings.local.json 폐지 권장** — 진짜 머신-특정만. 기존 프로젝트는 사용자가 로컬→커밋 수동 이관(권한파일은 분류기가 에이전트 편집 차단) | 2026-06-30 | scripts/merge-permissions.mjs | templates/settings.json, templates/permissions/*.json, scripts/new-repo.sh, tests/merge-permissions-test.sh, ci-gate.yml, plugin.json, README.md |

| `pr-create` 스킬 신설(v0.13.0) — **base 자동감지 PR 생성 단일 프리미티브**. 노출: `feature-merge`가 `gh pr create --base develop` 하드코딩이라 develop 없는 main 기반 repo(develop 미사용 public repo·개인 repo 등 — team-harness 자체는 이후 develop 채택)에서 안 맞아 매번 맨손 `gh pr create`로 샘(이번 세션 team-harness PR들 실증). 해결: `pr-create`가 base를 동적 감지(`git ls-remote origin develop` 있으면 develop, 없으면 `origin/HEAD` 기본 브랜치, fallback main)→push·품질검증·PR 생성. `feature-merge` §3은 이 스킬에 **위임**(PR 생성 로직 단일 출처·중복 제거). `pr-create`는 PR 생성까지만 — 머지는 pr-review-gate/solo-merge가 소유. route-intent는 무변경(feature+커밋+PR없음→feature-merge가 base 무관하게 동작) | 2026-06-30 | plugins/harness-guard/skills/pr-create/skill.md | feature-merge/skill.md, plugin.json(v0.13.0), README.md |

| team-harness도 **develop 통합브랜치 채택** — 다른 모든 repo(erp·siku·DriveTree·webhook-service)가 `기본=main + develop` 클래식 gitflow인데 team-harness만 main 단독이라 비일관 → develop 추가로 정렬. 기본 브랜치는 main 유지(다른 repo와 동일). ci-gate.yml 트리거를 `[main]`→`[main, develop]`로 확장(develop 대상 feature PR도 품질 게이트 경유, 안 그러면 develop이 무게이트 통합지점이 됨). 영향: team-harness 자체 작업도 feature→develop→(release)→main 흐름. **pr-create의 정당성은 유지** — develop 미사용 public 개인 repo + 오프라인/엣지 견고성 때문(team-harness 단독 사례가 아니라 일반 프리미티브). private+Free라 develop도 branch protection 없음(guard.sh 훅이 직접커밋 차단으로 대체)(⚠️ #73 public 전환·#79 브랜치 보호 표준으로 **대체됨** — 현재는 protection 적용) | 2026-06-30 | .github/workflows/ci-gate.yml | docs/decisions.md(pr-create 행), 다른 repo 브랜치 구조 |

| 맨손 `gh pr create`·`gh pr merge`를 **guard.sh(PreToolUse)에서 차단**(v0.14.0) — 반사적 스킬 건너뛰기 강제 교정. **증거**: pr-create 스킬을 만들어 머지한 *직후* 같은 세션에서 settings PR 3개(siku#72·DriveTree#56·webhook#47)를 또 맨손 `gh pr create`로 생성. 메모리·약속·스킬 존재만으로는 반사질이 안 막힘(라우터도 cwd가 repo일 때만 발동 → 부모 디렉터리서 여러 repo 작업 시 우회). **설계**: guard가 raw `gh pr create/merge`를 명령위치(시작·`;&|(` 뒤)에서 차단(grep/echo 문자열 언급은 통과), PR 생성·머지는 래퍼 스크립트 `scripts/pr-create.sh`(base 자동감지·push·생성)·`scripts/pr-merge.sh`(CI·스레드·mergeable 게이트 후 머지)로만. 래퍼 내부 gh는 **자식 프로세스라 PreToolUse 훅에 안 걸림** → 스킬은 래퍼 호출, 맨손은 차단. 모든 PR 스킬(pr-create·pr-review-gate·solo-merge·hotfix·release·milestone·goal)을 래퍼 경유로 갱신. **범위**: 반사질만 정조준 — 난독화 우회(temp 스크립트에 gh 숨기기)는 plan대로 제외(텍스트 규칙으론 불가). solo-merge 보호토글은 분류기 게이트로 별도 유지(래퍼에 안 넣음 — 분류기 우회 안 함) | 2026-06-30 | plugins/harness-guard/scripts/guard.sh | scripts/pr-create.sh, scripts/pr-merge.sh, 각 PR 스킬, tests/guard-test.sh, ci-gate.yml, plugin.json(v0.14.0) |

| guard PR 차단 정밀화 + 스킬 래퍼 참조 통일(v0.14.1): (1) guard 분리자에서 **`|`를 공백 동반 시에만** 셸 파이프로 인정(`(^|[;&(]|\|[[:space:]])`) + **후행 앵커에 `) ; & |` 추가**(`([[:space:]);&|]|$)`). 동기: `|`를 통째 분리자로 두면 따옴표 속 정규식 alternation(`grep "…|gh pr create…"`)을 파이프로 오인하는 오탐(감사 grep이 라이브로 2회 차단). 1차로 `|`를 통째 제거했으나 /code-review verifier가 **그러면 `echo y | gh pr merge`·`cat body | gh pr create --body-file -`(실재 idiom)이 통과**함을 적발 → 공백 휴리스틱으로 절충(`| gh`=파이프 차단, `|gh`=alternation 통과). 후행 앵커 추가로 `$(gh pr create)`(원래부터 새던 갭)도 차단. **알려진 한계(보조장치)**: `echo y|gh pr merge`(무공백 파이프)·따옴표 속 명령은 못 잡음(따옴표 인식 불가 — 최종 강제는 계층0). (2) **feature-merge §3을 pr-create 스킬 위임→스크립트 직접 호출로 통일** — hotfix·release와 동일 경로(`bash scripts/pr-create.sh`), 첫 verifier가 짚은 **이중 품질검증**(feature-merge §2 + pr-create 스킬 §1)도 해소(§2 단일). 결과: PR 생성·머지하는 모든 스킬이 래퍼 스크립트를 일관 참조. guard-test 32/0(오탐 통과·공백파이프/서브셸 차단·&& 차단) | 2026-06-30 | plugins/harness-guard/scripts/guard.sh | skills/feature-merge/skill.md, tests/guard-test.sh, plugin.json(v0.14.1), README.md |

| 스킬 간 참조는 **3종으로 구분**(섞지 말 것): ① **메커니즘**(`gh pr create/merge` 실제 실행) = **래퍼 스크립트**(`bash scripts/pr-create.sh`·`pr-merge.sh`) — guard 강제 대상, 모든 PR 스킬이 이걸 참조. ② **절차 위임**(리뷰 게이트 = AI 리뷰 스레드 처리·사람 승인·CI watch·머지 오케스트레이션) = **스킬 참조**(`pr-review-gate N단계`) — 다단계+판단/상호작용이라 스크립트로 못 감쌈(그 안의 머지 1줄만 pr-merge.sh). ③ **워크플로 핸드오프**("다음은 /feature-merge") = **스킬 이름**. **②③를 래퍼 참조로 바꾸면 안 됨**(절차가 사라짐). 즉 "왜 어떤 건 스크립트, 어떤 건 스킬?"의 답: gh 실행=스크립트, 절차·핸드오프=스킬 | 2026-06-30 | docs/decisions.md | 전 PR 스킬(메커니즘=스크립트, 게이트=pr-review-gate 위임) |

| 스킬의 스크립트 참조 이식성: 절대경로 `/Users/grinvi04/team-harness/...`(사용자명 하드코딩 → 다른 PC서 깨짐)를 **`${CLAUDE_PLUGIN_ROOT:-$HOME/team-harness/plugins/harness-guard}` 폴백형**으로 통일. 사유: 훅(hooks.json)은 런타임이 `CLAUDE_PLUGIN_ROOT`를 주입하지만 **스킬 prose의 bash엔 그 변수가 없을 수 있어**, 변수 있으면 사용·없으면 `$HOME/team-harness`(클론 위치 관행)로 폴백 → 사용자명 무관 이식. 대상: pr-create.sh·pr-merge.sh·check-repo-sync.mjs 참조 9개 스킬. hooks.json은 런타임 변수가 보장돼 bare 유지 | 2026-06-30 | plugins/harness-guard/skills/*/skill.md | (포터블 경로 컨벤션) |

| AI 기록 위치 정책 표준화: **프로젝트 상태·결정·도메인 지식은 repo/GitHub에 누적, 도구 로컬 AI 메모리는 팀 공유 불필요한 개인 습관·행동교정에만 최소 사용**(팀 지식 저장소로 안 씀). 기록 위치: 플랜·스펙=`docs/specs/`, 백로그·할 일=GitHub Issues + Milestone(`/milestone`), 마일스톤 정의·진행률=GitHub Milestone + `docs/milestones/`, 작업로그·완료기록=git 히스토리 + CHANGELOG/릴리즈노트, 결정·도메인 지식·이유=`docs/decisions.md`, 로컬 AI 메모리=개인 습관만. 사유: 로컬 메모리는 다른 PC·세션·사람이 못 보고 유실 → 팀이 알아야 할 건 전부 repo/GitHub로. 기존 표준은 "생성 문서→docs/ 커밋"과 "결정→decisions.md + 메모리 최소"까지였고 백로그·작업로그 위치가 미문서화라 신규 repo가 상속 못 함 → ai-collaboration.md 기록 위치 표 추가 + templates/AGENTS.md 지침·표 행 갱신 + feature-add/modify 스킬의 메모리 저장 문구를 개인 습관으로 제한. 기존 프로젝트(erp·siku는 main에만, DriveTree·webhook 없음)는 decisions.md를 develop에 전파·생성 필요(후속) | 2026-06-30 | docs/ai-collaboration.md | templates/AGENTS.md, skills/feature-add/skill.md, skills/feature-modify/skill.md, (각 프로젝트 docs/decisions.md) |

| Vue·Next.js 스택 1급 지원: 신규 프로젝트에 Vue/Next.js를 쓰기로 → new-repo.sh에 **case 7(Next.js 단독 App Router)·8(Vue 3 Vite SPA)** 추가 + 전용 자산 신설. 기존엔 Vue는 case 1(node)에 뭉뚱그려져 `.vue` 경로·vue-tsc 룰 없고, typescript.md가 `eslint-config-next`(Next 전용)를 Vue에도 강제했고, Next.js 단독 case 자체가 없었음. 추가: `ci-gate-nextjs.yml`(next lint·type-check·build)·`ci-gate-vue.yml`(vue-tsc·vitest), `rules/stacks/nextjs.md`(RSC 경계·server actions·NEXT_PUBLIC_ 보안·middleware)·`vue.md`(script setup·vue-tsc·reactivity 함정·Pinia), `permissions/nextjs.json`·`vue.json`, merge-permissions RULE_TO_FRAGMENT(nextjs·vue). typescript.md lint을 프레임워크별 분기(Next=config-next, Vue=plugin-vue), ci-gate-node에 type-check 스텝. 테스트 AC8 락 | 2026-06-30 | scripts/new-repo.sh | templates/ci/stacks/ci-gate-nextjs.yml·ci-gate-vue.yml, templates/rules/stacks/nextjs.md·vue.md, templates/permissions/nextjs.json·vue.json, scripts/merge-permissions.mjs, typescript.md, tests/merge-permissions-test.sh |

| 한국어 UI/UX 표준 신설: 사용자 피드백(erp 작업 시 AI가 영어 직역체·서구 UX 기본값으로 폴백 — "쇼핑 카트"·"Save" 버튼·"~하시겠습니까?") 교정. 기존 표준은 디자인 토큰·WCAG만이고 한국 UX는 `₩0` 예시 1회뿐 → 막을 메커니즘 0. 추가: **`docs/korean-ux.md`(단일 출처 — 정착 용어집·해요체 마이크로카피·폼 포맷)**, `templates/rules/korean-ux.md`(path-scoped 룰 — AI가 UI 파일 편집 시 자동 주입), `/qa` **Agent C(한국어 UX 적합성 advisory** — 영어직역 버튼·직역용어·날짜/통화 포맷, 머지 차단 아님), frontend-design-standards §11·templates/AGENTS.md 행(포인터). new-repo.sh가 프론트 스택(typescript·vue·nextjs·rails)에 korean-ux 룰 복사(path-scoped라 비-UI repo 무영향, 영어 서비스는 삭제) | 2026-06-30 | docs/korean-ux.md | templates/rules/korean-ux.md, skills/qa/skill.md, docs/frontend-design-standards.md, templates/AGENTS.md, scripts/new-repo.sh |

| 의도 라우터 키워드 확장(v0.17.0) 즉시 리버트 — v0.16.0 복구: 증분2 폐기 결정(#46 행)과 동일한 오버트리거 4케이스를 라이브 실증 재확인("for 루프 고쳐줘"→loop, "장애인 접근성 고쳐줘"→hotfix, "DB 동기화 로직 만들어줘"→repo-sync, "task-notification 핫픽스 해줘"→hotfix). veto 목록(NON_ACTIONABLE)은 질문·분석 패턴만 잡고 일반 코딩 명령어의 substring 충돌은 막지 못함. **원칙 재확인: 상태 기반 라우팅(openPR·branch-type·spec)만 정밀도 100%, 키워드 확장은 두더지잡기(altitude).** 키워드 기반 라우팅 재시도 불필요 — plan 파일(ethereal-stirring-snowglobe.md) 해당 증분 비채택 확정. route-intent.mjs 확장 가능 방향은 branch name 기반(hotfix/* → hotfix, release/* → release-check) 상태 신호만 | 2026-07-03 | plugins/harness-guard/scripts/route-intent.mjs | tests/route-intent-test.sh, plugin.json |

| **v0.16.1 릴리즈 사건 교훈 — 리버트 PR은 실제 파일 diff를 검증**: v0.16.1 릴리즈에 리버트 대상인 v0.17.0 route-intent.mjs가 그대로 포함된 사건. 원인: 리버트 PR(#83)이 decisions.md만 추가하고 route-intent.mjs를 실제로 `git checkout v0.16.0 --`하지 않음. 이중 실패: ① route-intent-test.sh도 v0.17.0 기준 테스트를 포함해 v0.17.0 코드에서도 전 테스트 통과 → release-check false-positive. ② 오버트리거 방지 케이스(일반 코딩 지시 → inject=false)가 테스트에 없어 라우터 로직이 바뀌어도 회귀 감지 불가. **교훈①**: 리버트 PR 머지 전 `git diff origin/main -- <대상 파일>` 또는 `git show v<버전> -- <파일>`로 코드가 실제로 롤백됐는지 확인. **교훈②**: route-intent-test.sh에 오버트리거 방지 케이스(AC-2i~k: 일반 코딩 지시 → inject=false)를 포함 — 이 케이스가 없으면 키워드 로직 재도입 시 release-check를 통과. 수정: hotfix/fix-v017-in-main → v0.16.2로 재릴리즈(v0.16.1 태그는 수정 없이 존치) | 2026-07-03 | tests/route-intent-test.sh(AC-2i~k) | plugin.json |

| team-harness → **public 전환**: 결정 #47의 "예외: team-harness = private" 변경. 사유: repo-sync CI가 team-harness를 checkout할 때 private이면 각 프로젝트에 PAT(HARNESS_RO_TOKEN)을 등록해야 함 — 기밀 내용이 없는 거버넌스 메타 repo에 불필요한 운영 부담. public 전환 시 토큰 불필요·repo-sync 즉시 활성화 가능·GitHub Free branch protection도 선택 가능. 결정 #47의 나머지(개인 코드 프로젝트 = public+MIT, 신규 repo 하네스 준수)는 유지 | 2026-07-03 | onboarding.md | templates/ci/repo-sync.yml(token 줄 삭제), 각 프로젝트 .github/workflows/repo-sync.yml |

| repo-sync CI 활성화(4개 프로젝트): team-harness public 전환 후 erp·siku·DriveTree·webhook-service에 HARNESS_SYNC_ENABLED=true 설정. token 불필요라 HARNESS_RO_TOKEN secret 등록 불필요. templates/ci/repo-sync.yml의 `token:` 줄 삭제(public이면 불필요 주석 참조). 각 프로젝트 .github/workflows/repo-sync.yml도 동일 갱신 | 2026-07-03 | templates/ci/repo-sync.yml | erp·siku·DriveTree·webhook-service .github/workflows/repo-sync.yml |

| feature 브랜치 진입 계약 강제(v0.18.0, 감사 F5 remedy): guard.sh가 `git checkout -b`/`switch -c feature/<name>` 시 승인된 `docs/specs/<name>.md`(=`/plan` 산출) 부재 & `HARNESS_TRIVIAL=1` 면제 부재면 차단. 동기: feature-add가 `/plan` 하류로 설계됐으나 상류 spec을 **관례로만 전제**(강제 안 함) — AI가 "응 해"에 성급히 `/feature-add` 진입하는 실패모드(이 세션 실증, 감사 PR #99 F5). **상태 기반(파일 존재)** 강제라 route-intent 오버트리거(substring, #46·#69)와 달리 정밀. 발단(브랜치 생성)만 게이트 — 기존 브랜치 계속·fix/hotfix 무관(guard-test 39/0). 명시 면제 토큰으로 "계획 건너뛰기=침묵 기본값"을 "의식적 행위"로 전환. 공유 guard라 전 소비 repo 적용(MINOR). 정직한 한계: 보조장치(난독화 우회 가능, 최종강제=계층0). F3·F4(solo-merge·release 유사 패턴)는 보상게이트 有로 별건. 버전: 0.17.0은 리버트로 burned(#69) → 0.18.0 | 2026-07-04 | plugins/harness-guard/scripts/guard.sh | skills/feature-add/skill.md, tests/guard-test.sh, plugin.json(v0.18.0), README.md |

| **브랜치 보호 표준(솔로)**: 전 repo public 전환(#73) 후 main·develop에 branch protection 적용 — **required status checks + force-push/삭제 차단 + 대화 resolve, 승인요건 0 · enforce_admins=false**(⚠️ enforce_admins 값은 아래 "브랜치 보호 enforce_admins=false→true(정정)" 행에서 **=true로 대체됨** — 정본=true). #47/#53의 "private+Free라 protection 없음 → guard.sh/solo-merge 의존" 전제를 **대체**. 사유: 솔로는 자기 PR 자기승인 불가 → 승인요건을 걸면 데드락이고, 자기승인은 무의미하므로 **CI 체크를 우회불가 게이트로** 삼고 소유자가 머지. enforce_admins=false라야 소유자가 릴리즈 머지 가능. config **단일 출처 = new-repo.sh(신규, STACK_CHECKS 명시등록) + scripts/set-branch-protection.sh(기존, 실제 check 자동감지 → 이름불일치 데드락 회피, `--check` 드리프트 모드)**. **solo-merge는 팀 모드(승인 재활성) break-glass로 강등** — 솔로 표준엔 불필요. pr-review-gate §4(승인)도 팀 모드 조건부. 적용: team-harness·erp·siku·webhook-service·drivertree × main/develop. 리뷰어 합류 시 승인요건 수동 1↑. repo-sync가 set-branch-protection --check로 드리프트 점검 | 2026-07-04 | plugins/harness-guard/scripts/set-branch-protection.sh | scripts/new-repo.sh, plugins/harness-guard/skills/solo-merge/skill.md, plugins/harness-guard/skills/pr-review-gate/SKILL.md, plugins/harness-guard/skills/repo-sync/skill.md, onboarding.md, plugin.json |

| **다이어그램 포맷 정본 = mermaid→PNG**(감사 E4, #106): `architecture-diagram-standards.md`(커스텀 SVG를 정본이라 규정)와 `readme-standards.md §4`(mermaid→`docs/architecture.png`)가 **정본을 서로 다르게** 규정한 충돌 해소. **정본=mermaid→PNG** — 텍스트 소스라 git diff·유지보수 우위, README.template·readme-standards가 이미 PNG. 커스텀 SVG(gen_arch_svg.py)는 **픽셀 단위 다크테마가 꼭 필요한 repo만** 쓰는 선택적 방식으로 강등(삭제 아님 — 기존 SVG 산출물 webhook·siku·DriveTree는 grandfather, 강제 마이그레이션 없음, 신규만 PNG 기본). architecture-diagram-standards.md 헤더·§1을 "선택적 방식"으로 재정합 | 2026-07-04 | docs/readme-standards.md | docs/architecture-diagram-standards.md |

| **migration-safety 짝-플래그 fail-fast**(엔지니어링 리뷰 S2, #109): `check-migration-safety.mjs`가 `--config`만 주고 `--migrations` 생략(또는 역) 시 나머지를 cwd에서 긁어 무관 마이그레이션/설정을 대상으로 오판(false-pass/fail)하던 문제. **한쪽만 명시하면 exit 2(사용법 오류)로 즉시 중단** — 둘 다 명시(정밀 모드) 또는 둘 다 생략([루트경로] 발견 모드)만 허용. 기존 호출부(templates/ci/migration-safety.yml=플래그 없는 발견 모드)는 안 깨짐. 테스트 3건 추가(11/0) | 2026-07-04 | scripts/check-migration-safety.mjs | tests/migration-safety-test.sh |

| **브랜치 보호 enforce_admins=false→true(정정)**: 직전 "브랜치 보호 표준" 행이 `enforce_admins=false`로 정했으나, 그 근거("소유자 릴리즈 머지 위해 false 필요")는 **승인요건이 있을 때만** 성립 — 승인요건 0에선 enforce_admins=true여도 소유자가 green이면 바로 머지(데드락 없음). false는 **CI-green 강제를 관례로 격하**(관리자가 required check 우회 가능 → CI red 머지 구멍, 실제 관측). true라야 "강제는 서버에" 원칙대로 CI-green이 소유자·에이전트에게도 우회불가 **계약**. 축 구분: solo-merge:14의 "enforce_admins 토글 무효"는 **review 요건** 축(그래서 solo-merge는 required_pull_request_reviews 직접 삭제), 이 결정은 **required status check(CI)** 축 — 별개. 긴급 break-glass(CI 인프라 장애, 드묾)는 required_status_checks 일시 완화(통상은 CI 고쳐 머지). set-branch-protection.sh `--check`가 enforce_admins off 드리프트도 리포트. 적용: 5 repo × main/develop 재적용 | 2026-07-04 | plugins/harness-guard/scripts/set-branch-protection.sh | scripts/new-repo.sh, docs/decisions.md(직전 행 정정) |

| **develop 전용 자동머지(개선2)**: develop CI-green PR을 `bash pr-merge.sh --auto <PR>`로 사람 재확인(auto-mode 분류기 프롬프트) 없이 머지 — 세션마다 develop·main 두 번 지시하던 마찰 제거. 메커니즘: 래퍼 머지를 막는 건 훅이 아니라 **분류기**(guard는 래퍼 통과·secret-scan은 유출 전용) → settings `permissions.allow`가 분류기 우회. **안전 1차 보증 = pr-merge.sh의 base 강제**(--auto는 base=develop만 허용, main은 exit 3 거부) — allow-rule 매처(`Bash(bash * pr-merge.sh --auto *)`)는 마찰감소용이라 fragile해도 최악=분류기 폴백(안전 불변). main/release는 --auto 대상 아님(계속 확인). enforce_admins=true(개선1)가 CI 서버강제라 develop 자동의 남은 리스크는 "의도"뿐(revertable). base 판정=순수함수 분리 단위테스트(pr-merge-auto-test 3/0). team-harness는 CLAUDE.md 부재라 정책을 code-review.md에 문서화. classifyAllShell(전역 분류)·훅 완화는 비채택 | 2026-07-04 | docs/specs/develop-auto-merge.md | plugins/harness-guard/scripts/pr-merge.sh, templates/settings.json, .claude/settings.json, docs/code-review.md, tests/pr-merge-auto-test.sh, ci-gate.yml, plugin.json |

| **team-harness 자기 AGENTS.md·CLAUDE.md 신설(self-dogfooding)**: 소비 repo는 모두 AGENTS.md+CLAUDE.md를 갖는데 team-harness 자신만 없었다(README.md만) — 표준을 담는 repo라는 암묵적 면제였으나, 자기 guard·branch protection·표준의 적용 대상이면서 작업 계약을 안 가진 건 "관례이지 계약" 결함. AI가 team-harness에서 일할 때 커밋 스타일·테스트 명령·버전 bump를 매번 추론하던 것을 명시 계약으로 전환. ⚠️ **`templates/AGENTS.md`(소비 repo용 템플릿)와 별개** — 루트 AGENTS.md는 team-harness 자신의 규약(정체성=거버넌스 플러그인 repo, 스택=bash/mjs/md, 빌드=구문·JSON·tests/*.sh, 기록위치·버전정책 자기 dogfood). CLAUDE.md는 @AGENTS.md + Claude 전용(git-flow 스킬·develop-auto 머지 포인터). 중복 아니니 지우지 말 것 | 2026-07-04 | AGENTS.md | CLAUDE.md, docs/decisions.md |

| **하네스 전면 감사 수정 릴리즈 v0.20.0**(harness-full-audit — 6축 감사 26건): medium 17·low 9를 P1~P5 테마 PR로 수정 — guard 정규식 우회4·과차단1(T2) · 안전게이트 fail-open 4(T3) · 솔로표준 문서 드리프트(T1) · load-bearing 판정로직 seam+테스트(T4) · provisioner↔verifier 대칭(T5). **결정: alembic 다중 head = (a) 게이트 제공**(사용자 확인) — check-repo-sync가 alembic 감지 시 `alembic-heads`를 required로 기대하므로 new-repo가 자기-스킵형 `templates/ci/alembic-heads.yml`을 대칭 스캐폴드(#39 "정적게이트=Flyway 전용, Alembic 다중head=별도 CI점검"의 실체화). check-repo-sync 감지·ruleMap에 nextjs·vue 추가(신규 룰문서 대칭). 버전 0.19.3→0.20.0(develop-auto MINOR 부채 흡수). | 2026-07-04 | docs/specs/harness-full-audit.md | plugins/harness-guard/scripts/{guard,pr-merge,pr-create,set-branch-protection,check-repo-sync}, scripts/new-repo.sh, templates/ci/alembic-heads.yml, plugin.json, README.md |

| **자동머지 안전계약 강화 v0.21.0**(감사 F 후속 — 실측이 드러낸 fail-open 2건): F 실측에서 team-harness·erp·siku가 `required_status_checks=null`이었고, 이 상태에서 `pr-merge.sh --auto`가 "required check 없음 → 통과"로 **CI-green을 강제 안 함**을 발견(auto 안전전제 붕괴). **(1) `--auto`는 required check 없음(none)이면 거부**(fail-CLOSED — 무인 자동머지는 서버-강제 CI 전제, 수동 머지는 사람 책임하 허용). **(2) `set-branch-protection.sh --contexts a,b,c`** 추가 — base 머지커밋엔 check-run이 없어 자동감지가 빈 목록이 되는 기존 repo 리메디에이션을 래퍼로 수행(PR-head 자동감지는 siku의 `Vercel Preview Comments` 같은 비-게이트를 등록하므로 배제). 5 repo × main/develop required check 등록 완료(실측 드리프트 0). TDD(pr-merge-auto +5·set-branch-protection +4) | 2026-07-04 | plugins/harness-guard/scripts/pr-merge.sh | plugins/harness-guard/scripts/set-branch-protection.sh, plugin.json, README.md |

| **릴리즈 보안검토가 P1 guard 회귀 포착(A5b)**: v0.21.0 릴리즈 전 `security-review`가 P1(A5) guard 커밋 정규식의 **우회 회귀**를 발견. A5가 과차단(`git log --grep=commit`)을 없애며 commit을 서브커맨드로 좁힐 때 `-C <dir>`만 허용 → `git -c user.name=x commit`·`git --no-pager commit` 같은 **임의 git 전역옵션 + commit**이 main/develop 직접커밋 차단을 우회(PoC 실증 exit 0). 수정: 정규식이 commit 앞의 임의 전역옵션(값-분리 플래그·단일 플래그)을 허용하되 서브커맨드가 commit이어야 매치(과차단 방지 유지). **교훈: guard 정규식 좁히기는 반드시 '여전히 차단돼야 할 변형' 회귀 케이스를 동반**(guard-test에 flag-bearing commit 케이스 부재가 CI 통과 원인) — A5b 5케이스 추가. 릴리즈 게이트(security-review)의 유효성 실증. 0.21.0 미출시 배치라 버전 흡수(별도 bump 없음) | 2026-07-04 | plugins/harness-guard/scripts/guard.sh | tests/guard-test.sh(A5b) |

| **스킬 발견성 버그 — git-flow 스킬이 소문자 `skill.md`(v0.21.1)**: team-harness 세션에서 `/release`·`/feature-merge` 등 플러그인 스킬이 **발견 자체가 안 되던** 구조적 결함. Claude Code는 스킬 매니페스트를 **`SKILL.md`(대문자)**로만 발견하는데(활성 frontend-design 확인), `commands/`→`skills/` 이전(F1/F2) 시 14개 git-flow 스킬이 소문자 `skill.md`로 남음(`pr-review-gate`만 대문자였음). 그 결과 하네스의 "스킬 강제(route-intent 주입 + 슬래시 스킬)" 계층이 무력 → AI가 재량으로 래퍼를 골라 쓰는 실패모드로 회귀. 수정: 14개 `git mv skill.md SKILL.md` + 기능 참조(harness-review.js DIMENSION 경로, ai-collaboration.md) 갱신 + **발견성 회귀 테스트**(git ls-files 대소문자 정확검사, macOS 케이스-무관 FS 우회). **별개 환경 이슈(비-repo)**: 플러그인이 `projectPath=/Users/grinvi04/project` local scope로 설치돼 `~/team-harness` 세션엔 미로드 + 캐시 stale(0.17.0) → dogfood하려면 team-harness에도 활성화+캐시 갱신 필요(사용자 환경). 교훈: **강제 계층 자체가 강제 없이 재량에 의존**하던, 이 감사가 다룬 결함 부류의 실증. | 2026-07-04 | plugins/harness-guard/skills/*/SKILL.md | tests/skill-discovery-test.sh, .claude/workflows/harness-review.js, docs/ai-collaboration.md, plugin.json, README.md |

| **skills↔guard 충돌 해소 = 브랜치/PR 경유(메타커밋 예외 없음)**(엔지니어링 리뷰 K, #109): goal·milestone·loop·feature-modify가 `develop`/`main`에서 직접 커밋하다 자기 guard에 막히던 충돌. 방향 후보 **(a) 브랜치/PR 경유 vs (b) guard 메타커밋 예외**에서 **(a) 확정** — 근거: "main/develop 직접커밋 금지·강제는 서버에" 일관성 + guard 예외는 grep 기반이라 우회 구멍이 됨 + `백로그=Issues·스펙=docs/specs`(AGENTS.md) 정합. 구현(#110, 이 세션 검증): **K2** feature-modify가 feature/* 생성 시 F5 plan-gate 발동을 명시(`/plan` 선행 or `HARNESS_TRIVIAL=1`/`fix/*` 면제) · **K3** goal/milestone은 `docs/goals`·`docs/milestones`를 직접커밋 안 함(GitHub Milestone 정본 + `templates/gitignore.snippet`이 소비 repo에 로컬 대시보드 gitignore 제공) · **K4** loop 0-0 브랜치 가드가 보호 브랜치 체크포인트 커밋 거부(작업 브랜치/`--no-commit`) · **K5** goal/milestone 조회에 `?state=all`(동명 closed 마일스톤 422 방지). K1(solo-merge 보호설정 저장·복구)은 #111에서 별건 해소 | 2026-07-05 | docs/code-review.md | plugins/harness-guard/skills/{feature-modify,goal,milestone,loop}/SKILL.md, templates/gitignore.snippet |

| **모델 티어링 현행화 — 메인 = Opus 기본**(#30 "Sonnet 메인 기본" 대체): Opus 4.8 이후 Anthropic 공식 권장이 **"Start with Opus"**로 전환 — Max·Team Premium·Enterprise·API 계정 default = Opus 4.8(Pro·Team Standard = Sonnet 4.6). 과거 표준("메인=Sonnet 기본")은 Sonnet이 합리적 기본이던 시절 서술이라 역행 → 현행화. **3-tier 개념·서브에이전트 티어링(enforce-subagent-model 훅)은 유지, 메인 세션 기본만 Sonnet→Opus** 정정. 비용 인식은 `opusplan`(계획=opus·실행=sonnet)·effort 튜닝·세션 위생(`/clear`·`/compact`) — 다운그레이드가 아님. 발단: 세션 비용($80) 검토에서 사용자가 "현재 Opus가 기본·recommended"임을 지적, 공식 문서로 확인. 기준: Claude Code *Model configuration* "Start with Opus"(2026-07 확인). | 2026-07-05 | docs/model-tiering.md | docs/decisions.md(#30 대체) |

| **비용 위생 = model-tiering 실전 결정표**(Claude 전용): 위임 정책을 추상 지침에서 **카테고리→구체예시→티어/도구 결정표**로 강화(읽기·검색=Explore/haiku·빌드=general-purpose/sonnet·검증/설계=opus·무거운 다단계=Workflow·단일 trivial만 인라인). Rules 2·3·5는 표로 축약(단일 출처). **강제 불가**(메인 AI의 위임 판단은 훅으로 못 막음 — 모든 인라인 읽기 차단은 오케스트레이션 불가)라 "따르기 쉽고 오해석 어렵게"가 최선. **진짜 결정론적 레버 = Workflow(stage별 모델)·opusplan(도구 기능, team-harness 밖)**; 문서는 이를 가리킴. tool-agnostic 추상화·강제 훅(T1 서브에이전트 훅 편입·T2 넛지) 폐기 — 지금은 Claude 전용, Codex/Gemini는 나중 재엔지니어링. 발단: $80 세션 opus 98%·sonnet/haiku 바닥 = 추상 지침 rationalization('자잘' 확대 적용)으로 읽기·빌드까지 opus 인라인한 실증. | 2026-07-05 | docs/model-tiering.md | docs/decisions.md |

| **팀 모드 브랜치 보호 = `set-branch-protection.sh --approvals N`**(v0.22.0 · 솔로 표준의 opt-in 확장, 대체 아님): 3–10인 팀 도입을 위해 브랜치 보호에 리뷰 승인 요건을 거는 옵션 추가. **승인은 main에만**(develop은 0 유지 → `pr-merge.sh --auto` 무프롬프트 머지 보존), 팀 기본에 `dismiss_stale_reviews=true`(승인 후 push 시 stale 승인 무효 — 승인 요건 우회 구멍 차단). `--check`의 승인 축은 **정보성**: `--approvals` 미지정 시 개수 무관 통과(승인↑는 *더 강한* 보호라 드리프트 아님 → `/repo-sync` 팀 repo 오탐 제거), 명시 시에만 baseline 판정(`0`=솔로엄격·정확히0 · `N`=팀·appr≥N). 적용 payload는 순수 함수 `reviews_json`으로 seam화(테스트 커버 — 무테스트 적용부 제거). **new-repo.sh 무변경**: day-1 소유자 1명에게 승인1은 self-approve 불가 데드락이라(GitHub 2026 변경: review 요건은 enforce_admins 무관 항상 강제, `solo-merge/SKILL.md`), 팀 승인은 멤버 합류 후 이 스크립트로 올림. 브랜치 보호 표준 행의 "리뷰어 합류 시 수동 1↑"가 이 플래그로 구체화. 클라이언트 게이트(`pr-review-gate`·`solo-merge`)는 `reviewDecision` 동적 판정이라 무변경. 발단: 새 회사(GitHub·한국인 3–10인 팀) 도입 준비 — 감사에서 유일 갭이 "솔로 전용 보호"로 확인. | 2026-07-05 | docs/specs/team-mode-branch-protection.md | plugins/harness-guard/scripts/set-branch-protection.sh, tests/set-branch-protection-test.sh, docs/{code-review,onboarding,harness-maintenance}.md, plugins/harness-guard/skills/repo-sync/SKILL.md |

| **stale `/goal` 스킬 dir 제거**(v0.22.2 · #31 rename 완결): `skills/goal/`은 #31(2026-06-23)에서 `/milestone`로 rename(내장 `/goal`=세션 stopping-condition과 이름 충돌 회피)했으나 옛 dir이 삭제 안 돼 잔존 — 플러그인에 `/goal`이 계속 등록돼 #31이 피하려던 내장 충돌을 재현하고 있었다. `milestone/`이 `goal/`의 **기능 상위집합**(동일 Phase C/S/B·동일 서브에이전트 모델 + '내장 /goal 병용' 절 추가)이고 goal 참조는 코드·테스트·plugin.json 어디에도 없어(모든 `/goal` 언급은 내장 것) 안전 제거. 이로써 스킬 dir 실물도 14개 = intro.html 라벨(별건 정정)과 정합. intro milestone 카드의 "별칭 유지" 서술도 제거(별칭 없음 — 전이중 SKILL.md는 정상 alias가 아니라 충돌원). 발단: intro.html 검증 Workflow(3렌즈)가 스킬 라벨(15)-실물(14 카드) 불일치를 적발하며 드러남. | 2026-07-05 | plugins/harness-guard/skills/milestone/SKILL.md | plugins/harness-guard/skills/goal/(삭제), docs/intro.html, plugins/harness-guard/.claude-plugin/plugin.json, README.md |

| **pr-merge 후 로컬 head 브랜치 자동 정리**(v0.23.0): 머지는 서버에서 일어나 원격 head는 `--delete-branch`(+repo `delete_branch_on_merge=true`)로 삭제되지만 **로컬 복사본이 남아 누적**(세션 종료 점검에서 34개 발견). `pr-merge.sh`가 머지 성공 후 로컬 head를 삭제하되, **origin/<base>에 브랜치 tip이 포함(=머지됨)일 때만** `-D`(미머지 로컬 커밋 유실 방지 — `git merge-base --is-ancestor` 안전검사, 미포함이면 보존+안내). 현재 그 브랜치 위면 base로 이동 후 삭제(순수 함수 `merge_cleanup_checkout` seam+테스트). 원격 삭제는 기존대로 무변경. 발단: 사용자가 "머지 시 로컬도 자동 삭제되면 좋겠다" 요청 — 원격은 이미 자동. | 2026-07-05 | plugins/harness-guard/scripts/pr-merge.sh | tests/pr-merge-auto-test.sh, README.md, docs/intro.html |

| **아키텍처 다이어그램 자동재생성 훅 표준화**(v0.24.0): `docs/gen_arch_svg.py` 저장 시 SVG를 자동 재생성하는 PostToolUse 훅(`regen-arch-svg.sh`)이 **4개 소비 repo(erp·webhook·DriveTree·siku)에 완전 동일 복붙**돼 있었으나 team-harness에 단일 출처가 없었다(생성기는 `templates/gen_arch_svg.py` 정본이 있으나 훅은 미소스) — 정확히 하네스가 막으려는 copy-paste 드리프트. 훅을 `templates/hooks/regen-arch-svg.sh`로 정본화하고 `architecture-diagram-standards.md §9`에 배선 절차(settings.json PostToolUse 스니펫) 추가. new-repo 스켈레톤엔 강제 안 넣음 — 다이어그램은 §9의 의도된 후속 단계라 그때 템플릿에서 복사. 발단: siku 미커밋 작업(자기 다이어그램+훅)을 커밋하려다 사용자가 "재사용 부분은 team-harness에서 해야 하는 것 아닌가" 지적 → 훅이 미소스 복붙임을 확인. (repo-sync 드리프트 점검·소비 repo 정합은 후속.) | 2026-07-05 | docs/architecture-diagram-standards.md | templates/hooks/regen-arch-svg.sh, plugins/harness-guard/.claude-plugin/plugin.json, README.md, docs/intro.html |

| **템플릿 생성기 ruff-clean**(v0.24.1): `templates/gen_arch_svg.py`가 ruff 미준수(`def l`=E743·미사용 `n`=F841·긴 f-string=E501·미포맷)라, python 소비 repo가 §9대로 복사하면 CI(ruff check + format --check)가 막혔다(webhook 실증: 복사본을 수동으로 E743 noqa·E501 분할·ruff format해야 통과). 템플릿을 ruff-clean화(E743 noqa·미사용 변수 제거·wrap() 연결 f-string 재작성으로 E501 해소·ruff format) — **생성 svg 출력은 불변**(베이스라인 diff 0으로 검증). 이제 python repo가 복사해도 즉시 CI 통과. 방금 v0.24.0 훅 표준화의 완결. 발단: webhook 생성기 커밋 중 템플릿 자체가 CI-비준수임을 발견. (team-harness CI는 python 미검사라 이 정합은 로컬 ruff로 검증 — 향후 CI 가드는 후속 후보.) | 2026-07-05 | templates/gen_arch_svg.py | plugins/harness-guard/.claude-plugin/plugin.json, README.md, docs/intro.html |

| **템플릿 ruff CI 가드**(v0.24.1 후속): v0.24.1이 "team-harness CI는 python 미검사라 향후 CI 가드 후속 후보"라 남긴 것을 완결 — `ci-gate.yml` quality 잡에 `templates/gen_arch_svg.py`의 `ruff check`(E,F,W·line-length 100) + `ruff format --check`(ruff==0.15.15 핀)를 추가. 이제 템플릿 편집이 ruff를 깨면 team-harness CI가 막아 python 소비 repo의 복사-후-CI실패 재발을 방지. team-harness 자체 CI 변경이라 소비 repo 미영향 → 버전 bump 없음. | 2026-07-05 | .github/workflows/ci-gate.yml | — |

| **new-repo 부트스트랩 데드락 수정**(v0.24.2 · dry-run 발견): 도입 리허설(버릴 test repo로 실전 플로우 검증)에서 발견 — `new-repo.sh`가 CI 워크플로 push **전에** main(README로 이미 존재)에 required-check 보호를 걸어, 초기 설정 커밋 push가 `required status checks are expected`로 거부되는 **day-1 블로커**. `apply_protection`에 "워크플로가 원격 브랜치에 있을 때만 보호 적용"(develop의 '원격 없으면 스킵'과 대칭) 가드 추가 → 보호는 설정 push 뒤에 걸림. onboarding.md 부트스트랩 순서 재작성 + `git commit --no-verify`가 **서버 branch protection을 못 뚫는다**(로컬 훅만 우회)는 틀린 안내 정정. dry-run에서 팀 모드(main 승인1·develop 승인0·enforce_admins)도 실전 검증(솔로만 dogfood했던 것). | 2026-07-05 | scripts/new-repo.sh | docs/onboarding.md, plugins/harness-guard/.claude-plugin/plugin.json, README.md, docs/intro.html |

| **/repo-sync 아키텍처 다이어그램 소스 점검**(v0.24.2): `check-repo-sync.mjs`에 arch 다이어그램 자산 점검 추가 — `docs/architecture.svg`(산출물)가 있는 repo에 `docs/gen_arch_svg.py`(소스)가 없으면 **WARN**(재생성 불가한 소스-less 산출물, webhook이 그 상태였음). severity=warn(비차단·다이어그램은 선택), applicable=산출물 보유 repo만(없으면 스킵). 생성기는 repo별 커스텀이라 템플릿과 내용 대조는 불가 — "소스 존재" 여부만 점검. 발단: dry-run 후속으로 아까 노트했던 "/repo-sync arch 미점검"을 완결. | 2026-07-05 | plugins/harness-guard/scripts/check-repo-sync.mjs | — |

| **경량 repo 면제 `.harness-lite`**(v0.25.0): 문서/경량 repo에 dev git-flow 강제(main/develop 직접 커밋·feature plan-gate·force push·맨손 gh pr create/merge)가 과적용되던 것을 repo 루트 `.harness-lite` 마커로 opt-out. 마커 있으면 guard.sh가 git-flow 5블록을 스킵(LITE 플래그). **파괴적 안전가드(rm -rf 코어·검증기 삭제·reset --hard·npm -g)는 경량 repo에도 유지** — 게이트 무력화 방지. 동기: 원칙 "의식의 무게를 산출물별로 right-size" — 문서 플레이북 등에 풀 git-flow는 역방향 오버엔지니어링. 마커=의식적 opt-out(HARNESS_TRIVIAL의 repo 스코프판). 트레이드오프: 마커를 코드 repo에 잘못 두면 git-flow 게이트 전면 면제 → 의식적 파일 생성 필요라 HARNESS_TRIVIAL과 동급 리스크(최종 강제는 계층0=branch protection·CI). guard-test.sh에 LITE 10케이스 추가(82/82 통과). | 2026-07-05 | plugins/harness-guard/scripts/guard.sh | plugin.json(0.25.0), tests/guard-test.sh |

| **모델 티어링 훅·verifier 에이전트 플러그인 승격**(v0.26.0): 개인 `~/.claude/hooks/enforce-subagent-model.py`(unversioned·untested·머신로컬)를 harness-guard 플러그인으로 승격 — `scripts/enforce-subagent-model.py` + hooks.json `matcher: Agent` 배선 + `tests/enforce-subagent-model-test.sh`(14케이스). 정책(model-tiering.md)은 versioned repo인데 강제는 개인 훅이던 "정책·게이트 분리" 갭 해소 → guard처럼 versioned·tested·설치로 공유. 개인 전용이던 `verifier.md`(opus 검증 에이전트)도 security-reviewer처럼 플러그인 `agents/`로 승격. **하드포스 정책 확정**(#3): TIER 타입은 넘긴 model 무시하고 강제(타입=티어) — opt-in 탈출구 안 만듦(LLM 비용부풀리기 차단). opus+파일쓰기는 메인 인라인/Workflow로. 로그는 `~/.claude/hooks/subagent-model.log` 유지(감사 연속). 훅 보강: log makedirs·tz-aware·no-op 스킵. **활성화: 릴리즈+재설치 필요(코드 변경). 활성 후 개인 훅·settings 등록·개인 verifier.md 제거(중복 방지).** | 2026-07-05 | plugins/harness-guard/scripts/enforce-subagent-model.py | hooks.json, tests/enforce-subagent-model-test.sh, plugins/harness-guard/agents/verifier.md, plugin.json(0.26.0), docs/model-tiering.md, docs/intro.html |

| **감사 로그 규약 문서화 + 훅 로그 로테이션**(v0.27.0): (A) 하네스 자체 감사 로그(`guard-block.log`·`subagent-model.log`)의 위치·읽는법·**VCS 밖 규약**(머신로컬·cwd/명령 히스토리 포함이라 repo 커밋 금지, 소스=훅만 versioned, 팀 집계는 중앙 ship)을 operations.md에 문서화. 발단: "감사로그도 repo에?" 질문 → 데이터(민감·churn) 아닌 규약만 넣는다. (B) `guard.sh`·`enforce-subagent-model.py` log에 256KB 초과 시 최근 절반 보존 로테이션(무한 증가 방지). 테스트 무영향 확인. | 2026-07-05 | docs/operations.md | plugins/harness-guard/scripts/guard.sh, plugins/harness-guard/scripts/enforce-subagent-model.py, plugin.json(0.27.0) |

| **서브에이전트 모델 티어링 2단 분리 — TIER(하드포스) + DEFAULT(기본값 채움)**(v0.28.0): `enforce-subagent-model.py`에 `DEFAULT` dict 신설(`harness-guard:verifier`·`harness-guard:security-reviewer`→opus). TIER(비용 다운그레이드, 넘긴 model 무시)와 성격이 반대(품질 하한 보장)이자 정책도 반대(명시값은 그대로 존중 — 의식적 다운그레이드 허용)라 같은 dict로 섞지 않음. 발단: 메인을 Sonnet으로 낮춰 실측한 결과 verifier/security-reviewer가 **agents/*.md의 `model: opus` frontmatter 덕에 이미 opus로 실행됨을 확인**(훅은 관여 안 함, `skip` 로그) — 즉 훅 하드포스 갭은 없었음. 그런데도 DEFAULT를 추가한 이유: frontmatter가 **단일장애점**이기 때문 — 새 특권 타입 파일 작성 시 사람이 `model: opus`를 깜빡할 수 있고, Claude Code 자체가 frontmatter의 `model` 필드를 안 지킨 실사례가 공개 GitHub 이슈로 다수 확인됨(anthropics/claude-code #44385·#18346·#43869, 2026-07 gh api로 직접 조회·검증 — 지어낸 근거 아님). 이 패턴(명시값 존중 + 미지정시만 채움) 자체도 공개 repo에 실제 채택 사례 있음을 gh api로 직접 검증(Levisek/claude.config `model-resolver.js`, rubicanjr/FinCognis `model-router.ts` 등, 지어낸 리서치 결과 아닌지 repo 존재·파일 내용까지 재검증). 기존 `verifier`/`security-reviewer` 미터치 테스트가 실제 subagent_type(`harness-guard:` 네임스페이스 포함)이 아닌 bare 이름으로 작성돼 있던 기존 드리프트도 이번에 정정. 테스트 14→22케이스(DEFAULT 채움·명시값 존중·falsy 엣지·malformed JSON 안전성 추가), 245/245 전체 스위트 회귀 없음. | 2026-07-06 | plugins/harness-guard/scripts/enforce-subagent-model.py | tests/enforce-subagent-model-test.sh, docs/model-tiering.md, plugin.json(0.28.0) |

| **감사 로그 위조(log forging) 방지 — repr 이스케이프**(v0.28.0 릴리즈 전 편입): v0.28.0 release-check의 security-reviewer가 실제 프로브로 재현: `enforce-subagent-model.py`의 `log()` 호출이 `session_id`·`cwd`·`model`(오케스트레이터/입력 통제 가능값)을 개행 이스케이프 없이 평문 보간해, 개행 포함 값으로 감사 로그에 가짜 라인을 위조 가능했음(예: `model="haiku\nFORGED session=evil..."` → 로그에 진짜처럼 보이는 위조 라인 삽입). `{sid!r}`·`{cwd!r}`·`{passed!r}`(repr)로 교체해 개행이 이스케이프된 텍스트로만 남게 수정 — 라이브 재현으로 위조 라인 생성 안 됨 확인(실제 로그 라인 수 변화로 검증). `stype`는 기존에 이미 `!r` 처리돼 있었음(범위 밖). 테스트 1건 추가(22→23, 위조 시도 후 로그 실제 증가 라인 수=1 확인). 발단: 이 자체가 v0.28.0(서브에이전트 모델 DEFAULT 계층) release-check 도중 발견 — 별도 fix 브랜치로 분리해 같은 릴리즈에 편입(품질 게이트 우회 아님, LOW라 release-check는 통과했으나 감사로그 무결성 직결이라 즉시 반영 택함). | 2026-07-06 | plugins/harness-guard/scripts/enforce-subagent-model.py | tests/enforce-subagent-model-test.sh, plugin.json(0.28.0) |

| **훅 fail-open 계약 복원 + 감사 로그 테스트 격리**(v0.28.1, 패치): v0.28.0 배포 직후 "진짜 어려운 문제로 라이브 재검증"(verifier를 model 미지정으로 실제 스폰해 DEFAULT-fill 자체를 적대 감사) 중 발견된 2건. **(A) 훅 크래시**: `enforce-subagent-model.py`의 try/except가 `json.load()`만 감싸, JSON은 유효하지만 형태가 예상과 다른 입력(최상위 non-dict·`tool_input` non-dict·`subagent_type` unhashable)에서 uncaught 예외로 크래시 — docstring이 약속한 "조용한 fail-open+감사 로그"가 깨짐(로그도 안 남음). `main()`의 처리 로직을 `_process()`로 분리해 별도 try/except로 감싸고 실패 시 `log(f"shape-error {e!r}")` 후 fail-open하도록 복원. **(B) 감사 로그 테스트 오염**: 위 라이브 재검증 중 verifier가 레이스 컨디션 재현을 위해 **실 감사 로그(`~/.claude/hooks/subagent-model.log`)에 직접 시드를 주입**했다가 제거하는 과정에서 당일 세션의 진짜 감사 이력이 유실됨(되돌릴 수 없음, best-effort 로그이며 30일 자동삭제 대상 아님 — Claude 세션 로그와 별개인 우리 자체 커스텀 파일이라 명확히 정정). 근본 원인은 이 테스트 스위트 자체가 애초에 매 실행마다 실 로그에 직접 썼던 것(오늘 이전부터의 사전존재 결함, security-reviewer가 v0.28.0 release-check에서 "테스트 격리 LOW"로 이미 짚었었음) — `HARNESS_SUBAGENT_MODEL_LOG` 환경변수로 로그 경로 오버라이드 가능하게 하고, 테스트 스위트가 `mktemp` 임시 파일로 자체 격리(trap으로 정리)하도록 수정해 재발 차단. 테스트 23→27케이스(shape-error 3종 + 격리로그 기록 검증 1종 추가), 250/250 전체 스위트 회귀 없음, 실 로그 mtime 불변으로 격리 검증. | 2026-07-06 | plugins/harness-guard/scripts/enforce-subagent-model.py | tests/enforce-subagent-model-test.sh, plugin.json(0.28.1) |

| **guard.sh 안전가드 우회 잔여 8건 정규식 일반화 + fail-closed 강화**(v0.29.0): 전면 심층 헌트(가드 우회·fail-open 축, 재현 기반 적대검증)로 확정한 잔여 8건 — 형제 가드(commit·force-push)가 이미 방어하는 변형을 다른 가드가 **비대칭 누락**한 것. (H1) `git reset --hard`가 리터럴이라 이중공백·탭·인자후치·`git -C . reset --hard`로 우회 → 명령 위치 앵커 + 세그먼트 내 `--hard` 검사(순서·공백·-C·env-prefix 무관, `grep 'git reset --hard'` 문자열 언급 과차단도 함께 제거). (H2) `rm -rf .`/`rm -rf ..`가 정규화 case(`./*|../*`)의 맨몸 `.`·`..` 누락으로 프로젝트 루트·상위 삭제 미차단 → case에 `.`·`..` 추가 + '루트와 같거나 상위(루트 포함)' 비교로 강화(`rm -rf /`도 차단). (M1) main/develop 직접커밋이 env-prefix(`X= git commit`)·선행공백으로 우회 → 앵커에 `^[[:space:]]*`·env-assign 프리픽스 허용. (M2/M5) 핵심 디렉터리·검증기 삭제가 따옴표(`rm -rf "src"`)로 우회 → 대상 종단에 `["']?` 허용. (M7) force-push가 `git -C <dir>`를 놓쳐(commit은 A2에서 -C를 잡는데 push는 비대칭) 우회 → push 매처에 git 전역옵션 허용 + -C dir 기준 브랜치 판정. (fail-open) python3가 있으나 실행 실패 시 파싱이 조용히 빈 값이 되어 전 가드 우회 → TOOL 추출 rc 검사로 fail-closed. (log forging) 감사 로그 writer(guard.sh)가 session_id·cwd 개행 위조에 취약(v0.28.0 repr 픽스는 enforce-subagent-model.py에만 적용된 잔여) → tr로 개행·탭 정제. reverify 재현으로 우회 폐쇄 확인, guard-test 86→103케이스(우회별 RED→GREEN + fail-closed·위조방지·과차단방지 회귀), 전체 스위트 회귀 0. mv(리네임)·find 광역화는 기존 D2 테스트가 mv를 의도적 허용(오탐)이라 제외. GitHub #180. | 2026-07-06 | plugins/harness-guard/scripts/guard.sh | tests/guard-test.sh, plugin.json(0.29.0), README.md(배지) |

| **route-intent isSolo가 현재 브랜치 대신 PR base 브랜치 protection을 조회하도록 교정**(v0.29.1): 심층 헌트로 확정 — `collectState`의 isSolo 판정이 PR이 머지되는 base(develop/main)가 아니라 `git branch --show-current`(=feature/fix/release/hotfix)의 protection을 조회했다. feature 브랜치는 항상 비보호라 gh가 404 → `classifySolo(404)=true` → isSolo=true → `decide()`가 solo-merge(required review 일시 삭제 스킬)로 라우팅. 즉 develop이 보호된 '팀' repo에서도 열린 PR 위 '머지해'가 pr-review-gate가 아닌 리뷰-우회 경로를 능동 주입(release/hotfix도 동일 오라우팅). openPR 조회에 `baseRefName` 추가 + 순수함수 `soloProtectionRef({openPR,prBase})`로 조회 대상을 base로 한정(openPR 없음·base 불명이면 null→team-safe). route-intent-test 26→30(base 조회·no-PR·base불명 team-safe 추가). 전체 스위트 회귀 0. GitHub #181. | 2026-07-06 | plugins/harness-guard/scripts/route-intent.mjs | tests/route-intent-test.sh, plugin.json(0.29.1) |

| **check-migration-safety 단일 파일 다중 프로파일 문서 false-pass 차단**(v0.29.2): 심층 헌트로 확정 — S1 방어(비운영 프로파일의 out-of-order:true 불신)가 파일명(basename)으로만 비운영을 걸러, 단일 `application.yml` 안에 `---`로 구분된 다중 문서(spring `on-profile: test`/`prod`)로 프로파일을 담으면 파일명은 `application.yml`(운영 취급)이라 필터를 통과하고, OOO 스캔이 파일 전체를 훑어 test 문서의 `out-of-order:true`를 먼저 만나 크레딧 → 운영 프로파일이 false/미설정이어도 게이트 통과(대역 마이그레이션 운영 기동불가 클래스를 CI가 놓침). OOO 스캔을 문서 단위로 분리(`---`)해 비운영 on-profile 문서를 크레딧에서 제외(운영·프로파일無 문서만 신뢰), `.properties/.conf` 등 `---` 없는 파일은 단일 문서로 무변경 처리. 픽스처 bad-multidoc-profile 추가, migration-safety-test 13→14. 전체 스위트 회귀 0. GitHub #182. | 2026-07-06 | scripts/check-migration-safety.mjs | tests/migration-safety-test.sh, tests/fixtures/migration-safety/bad-multidoc-profile, plugin.json(0.29.2) |

| **check-repo-sync sentinel 매칭 전 주석 제거 — 비활성 게이트 오인 차단**(v0.29.3): 심층 헌트로 확정 — 필수 CI 게이트 존재 판정이 워크플로 전체 텍스트를 부분일치해, 주석 처리된(비활성) 게이트(`# TODO gitleaks`)도 존재 신호로 오인돼 드리프트가 미탐지됐다. wfList 구성 시 YAML 주석(라인시작·공백 뒤 `#`)을 제거해 주석된 게이트가 크레딧을 못 받게 교정. **범위 결정**: echo/텍스트 기반 sentinel의 느슨함(예: `run: echo "...gitleaks..."` 언급)은 제거하지 않음 — 하네스 자체 test-guard 워크플로가 `run: echo allow-test-removal`을 정당한 신호로 쓰므로(good 픽스처로 확인) echo를 언급으로 보고 제거하면 정당 신호가 깨진다. sentinel 검사는 functional 검증이 아니라 존재 신호이며 OK/WEAK 티어가 그 부정확성을 이미 인정 — echo-언급 벡터는 설계상 수용 한계로 남긴다(주석 벡터만 봉쇄). 픽스처 bad-sentinel-comment 추가(gitleaks가 주석에만 → MISSING), repo-sync-test 6→7. 전체 스위트 회귀 0. GitHub #183. | 2026-07-06 | plugins/harness-guard/scripts/check-repo-sync.mjs | tests/repo-sync-test.sh, tests/fixtures/repo-sync/bad-sentinel-comment, plugin.json(0.29.3) |

| **commitlint type-enum을 정본 code-review.md와 동일 집합으로 정합**(v0.29.4): 회귀 검토로 확정 — 품질 게이트(commitlint)가 정본으로 선언한 code-review.md와 실제 강제 규칙이 어긋나, 문서상 유효한 `perf` 타입 커밋이 required check(commitlint)에서 거부돼 PR 머지가 막히던 기능 불일치. 역으로 `style`·`ci`는 게이트만 허용. 양방향 드리프트를 union superset(`feat fix docs style refactor perf test chore ci`)으로 정합 — 양쪽 추가만, 제거 없음(기존 커밋 무영향). node 스크립트로 두 집합 동일 검증. GitHub #190. | 2026-07-06 | templates/commitlint.config.cjs | docs/code-review.md, plugin.json(0.29.4) |

| **스킬 정합성 2건 교정 — pr-create 솔로/팀 오라우팅 + release-check forward-only Alembic 오탐**(v0.29.5): 회귀 검토로 확정. (A) pr-create/SKILL.md가 머지 후속을 '솔로→solo-merge / 팀→pr-review-gate'로 안내했으나 정본(solo-merge·pr-review-gate SKILL)은 정반대 — solo-merge는 승인요건(1+) 걸린 팀 모드용 break-glass이고 솔로(승인0)는 직접 pr-merge.sh. 솔로 사용자를 승인우회 커맨드로 오도하던 것 교정. (B) release-check/SKILL.md의 forward-only 점검이 Flyway-scoped 정의(undo 파일 U{n} 금지)를 스택무관 'down/rollback 포함 시 ❌'로 적용 → Alembic의 정상 `downgrade()`(autogenerate 생성)를 전건 위반으로 거짓 차단. Flyway undo 파일로 스코프하고 Alembic downgrade()는 정상(금지는 downgrade base뿐)임을 명시. 둘 다 스킬 프롬프트·안내 텍스트 교정(로직 무변경). GitHub #191. | 2026-07-06 | plugins/harness-guard/skills/pr-create/SKILL.md, plugins/harness-guard/skills/release-check/SKILL.md | plugin.json(0.29.5) |

| **정합성 드리프트 4건 정정**(문서, 버전 무관): 회귀 검토 확정 — ① decisions #18(git-flow branch protection)에 '#79 솔로 표준(승인0)으로 대체됨' 마커 추가(자기 규약 line 10-11 준수, 승인 1+↔0 자기모순 해소). ② Node 표준 통일 — architecture-infra Docker 베이스 `node:22-alpine`→`24-alpine`(stack-guide '최신 LTS 24'와 정합, '로컬·CI·Docker 일치' 원칙). ③ frontend-design-standards korean-ux 룰 경로를 소스(`templates/rules`)→주입(`.claude/rules`) 관계로 정합(정본 korean-ux.md·#67과 일치). ④ README 라이선스 'Private'→'MIT'(2026-07 public 전환 #73 반영) + 루트 LICENSE(MIT) 파일 추가(#47·readme-standards 정책 충족). GitHub #192. | 2026-07-06 | docs/decisions.md, docs/architecture-infra.md, docs/frontend-design-standards.md, README.md, LICENSE | — |

| **guard.sh LITE 교차오염 + npm=값 우회 + force-push refspec 하드닝**(v0.29.6): 2차 심층 헌트로 확정. (LITE) `_LITE_ROOT`를 후행 cd 착지 cwd에서 산출해, 코드 repo(마커無) 명령 뒤에 `&& cd <아무 .harness-lite repo>`를 붙이면 LITE=1이 돼 main/develop 커밋·force·gh pr·plan 게이트가 전부 열리던 교차오염(A2 anti-bypass 자기무력화, plan 게이트는 서버 백스톱 無). LITE를 **후행 cd 이전에 캡처한 원래 repo(`_ORIG_ROOT`)** 기준으로 산출해 차단 — lite repo 자체에서의 정상 면제는 유지(회귀 없음 실측). (npm) `npm install --global=true`가 `(-g|--global)([[:space:]]|$)` 매처를 `=값`으로 우회 → `(=[^;&|[:space:]]*)?` 추가. (force-push) 목적지 매처가 `origin (main|develop)`만 잡아 완전수식 refspec(`refs/heads/main`·`HEAD:refs/heads/develop`)·non-origin remote(`upstream main`)로 우회(서버 allow_force_pushes=false 백스톱이나 로컬 정본화) → `([[:space:]]|:|\+)(refs/heads/)?(main|develop)`로 일반화. OLD/NEW 대조로 우회→차단 실측, guard-test 103→112(교차오염·npm=·refspec + 과탐방지 featmain). 전체 스위트 회귀 0. GitHub #196. | 2026-07-06 | plugins/harness-guard/scripts/guard.sh | tests/guard-test.sh, plugin.json(0.29.6) |

| **check-migration-safety 프로파일 적용성 의미론 판정 — 부정프로파일 false-pass + 리스트 false-FAIL**(v0.29.7): 2차 심층 헌트로 확정. #182의 nonprod 제외가 토큰 매칭(NONPROD_TOKEN_RE)만 써서, (#2) `on-profile: "!prod"`(운영 제외 전 환경) 문서의 out-of-order:true가 nonprod 토큰이 없어 크레딧 → 운영 미적용인데 게이트 통과(운영 부팅실패 클래스 놓침, OLD=0 실측). (#7) 역으로 `test | prod`(운영 포함) 문서를 nonprod 토큰(test) 있다고 제외 → 안전 config를 거짓 실패(OLD=1 실측). `isProdApplicable(val)` 신설로 의미론 판정: `!prod`/`!production`→미적용(제외), `prod` 토큰 포함→적용(포함), nonprod만→제외, 프로파일 없음(default)→적용. 픽스처 bad-neg-prod-profile(→FAIL)·good-list-profile(→PASS) 추가, migration-safety-test 14→16. OLD/NEW 대조로 양방향 교정 실측. 전체 스위트 회귀 0. GitHub #197. | 2026-07-06 | scripts/check-migration-safety.mjs | tests/migration-safety-test.sh, tests/fixtures/migration-safety/{bad-neg-prod-profile,good-list-profile}, plugin.json(0.29.7) |

| **hotfix/release back-merge 실행가능화 + loop stuck 감지 교정**(v0.29.8): 2차 심층 헌트로 확정. (#3) hotfix·release Phase 4 back-merge가 `git checkout main` 후 `pr-create.sh --base develop`를 호출하는데, pr-create는 base 브랜치(main/develop)를 head로 거부(is_base_branch)하고 원 브랜치는 머지로 이미 삭제돼 **back-merge PR이 생성 불가**(hotfix가 develop에 영영 미반영되는 정합성 붕괴) → main 기준 `sync/backmerge-*` 브랜치를 만들어 그것을 head로 PR(sync/* 는 F5 무관). (#8) /loop stuck 감지가 `git diff --name-only`(누적 미커밋 변경)를 봐서 `--no-commit` 모드에선 항상 non-empty → 진전 없어도 stuck 영영 미발동 → MAX_ITER까지 무한 → 반복 시작 지문(TREE_BEFORE) 대비 종료 지문 비교로 교정(커밋 여부 무관, 양 모드 동작). 스킬 절차 문서 수정(실행 코드 아님, 테스트 하네스 없음 — 추적화). 전체 스위트 회귀 0. GitHub #198. | 2026-07-06 | plugins/harness-guard/skills/hotfix/SKILL.md, plugins/harness-guard/skills/release/SKILL.md, plugins/harness-guard/skills/loop/SKILL.md | plugin.json(0.29.8) |

| **게이트 견고성 3건 — set-branch-protection strict 감사 + alembic-heads 임계값 바인딩 + pr-merge classify_ci 상태컬럼 우선**(v0.29.9): 2차 심층 헌트로 확정. (#4) set-branch-protection `--check`가 `required_status_checks.strict`를 감사 안 해 strict=false(stale-green 머지 허용) 드리프트를 clean으로 보고 → classify_protection에 strict 파라미터 추가(false면 drift). (#5) alembic-heads-test가 계수 패턴만 YAML에 묶고 게이트 임계값은 자체 재구현만 해 YAML을 `-gt 2`로 열어도 green → YAML 실제 표현식 `"${heads:-0}" -gt 1`을 드리프트 가드로 assert. (REJ3) pr-merge classify_ci_gate가 rc≠0일 때 `gh pr checks` 출력 전체(실패 체크 이름 포함)를 에러토큰으로 grep해, 이름에 'GraphQL' 든 실패 required check를 fallback으로 오판(서버 required check가 최종 백스톱이나 로컬 정본화) → 상태 컬럼(TAB 2번째 필드)의 비-통과 상태를 에러토큰 검사보다 먼저 판정(fail), 실제 API 에러문(표 아님)은 fallback 유지. 테스트 set-branch-protection 27→30·alembic-heads 9→11·pr-merge-auto +3. 전체 스위트 회귀 0. GitHub #199. | 2026-07-06 | plugins/harness-guard/scripts/set-branch-protection.sh, tests/alembic-heads-test.sh, plugins/harness-guard/scripts/pr-merge.sh | tests/set-branch-protection-test.sh, tests/pr-merge-auto-test.sh, plugin.json(0.29.9) |

| **자기-회귀 교정(mjs 2건) — migration !test false-FAIL + check-repo-sync 인라인# false-MISSING**(v0.29.10): 자기-회귀 헌트(이번 세션 12 PR을 적대 재검증)로 확정. (#2) #197 isProdApplicable이 `!prod`만 부정 특수처리하고 대칭 `!test`/`!dev`(=운영 포함)를 누락 → substring `test` 매칭으로 nonprod 오판 → out-of-order 크레딧 제외 → 안전한 banded 마이그레이션 거짓 차단(OLD=1 실측). 부정 비운영 프로파일을 운영 적용(true)으로 처리 추가. (#6) #183 stripComments가 트레일링/인라인 `#`(예: `echo "issue #12 … allow-test-removal"`)를 제거해 따옴표 문자열 안 정당 sentinel까지 삭제 → false MISSING(머지 차단, OLD=1 실측) → 전체-라인 주석(`^\s*#`)만 제거로 축소(#183 목표는 전체-라인이라 유지). 픽스처 good-neg-nonprod-profile·good-sentinel-inline-hash 추가, OLD/NEW 대조로 회귀 교정 실측. 전체 스위트 회귀 0. GitHub #205. | 2026-07-07 | scripts/check-migration-safety.mjs, plugins/harness-guard/scripts/check-repo-sync.mjs | tests/migration-safety-test.sh, tests/repo-sync-test.sh, plugin.json(0.29.10) |

| **자기-회귀 교정(guard.sh 3건) — force-push 과탐 + reset wrapper 우회 + 명시refspec 오차단**(v0.29.11): 자기-회귀 헌트로 확정한 이번 세션 회귀. (#1) #200이 force-push 목적지 매처를 명령 전체에 grep해, feature force-push 시 rebase 인자·커밋메시지·주석의 main/develop 토큰에 오차단 + `-[a-zA-Z]*f`가 `--follow-tags`를 force로 오탐 → 목적지 판정을 push 세그먼트로 스코프, force 플래그를 `--force`/`[[:space:]]-…f…`로 정밀화. (#4) #180 reset 명령-위치 앵커가 `sudo/env/time/xargs git reset --hard` wrapper 프리픽스를 놓쳐 우회(무백스톱 파괴가드) → 앵커에 `[[:space:]]` 추가(인용-언급 보호 유지). (#5) develop 체크아웃서 명시 refspec(`origin feature/x`) force-push가 현재브랜치 절로 오차단 → push 세그먼트에 명시 refspec 있으면 현재브랜치 휴리스틱 스킵(bare push만 적용). OLD/NEW 대조 실측(#4 OLD0→NEW2, #1·#5 OLD2→NEW0, origin main force OLD2→NEW2 유지), guard-test 112→122. 전체 스위트 회귀 0. GitHub #204. | 2026-07-07 | plugins/harness-guard/scripts/guard.sh | tests/guard-test.sh, plugin.json(0.29.11) |

| **자기-회귀 교정(guard High) — 다중-push force 우회 + 감사로그 대문자 URL 마스킹**(v0.29.12): 6회차 자기-회귀 헌트로 확정. (High) #207의 force 재작성이 `PUSH_SEG=…| head -1`로 **첫 push 세그먼트만** 판정 → 체인된 다중 push에서 무해한 첫 push 뒤에 main/develop force-push를 붙이면(`git push origin feat && git push --force origin main`) 뒤 세그먼트가 미검사돼 통과(force 가드 완전 우회, OLD=0 실측). **모든** push 세그먼트를 개별 순회 판정(while read < grep -oE, head -1 제거)으로 교정 — 각 세그먼트 force+목적지 판정, 순서 무관 차단, 다중 feature push 과탐 없음(실측). (Low) deny() 감사로그의 URL 크레덴셜 마스킹 `(https?://)`가 대소문자 구분 → `HTTPS://user:pass@host` 대문자 스킴이 마스킹 우회돼 평문 로깅 → `[Hh][Tt][Tt][Pp][Ss]?://`로 대소문자 무관화. OLD/NEW 대조로 우회→차단 실측(다중-push OLD0→NEW2), guard-test 122→127. 전체 스위트 회귀 0. GitHub #208. | 2026-07-07 | plugins/harness-guard/scripts/guard.sh | tests/guard-test.sh, plugin.json(0.29.12) |

| **route-intent committed 판정 base를 develop 우선으로 교정**(v0.29.13): 6회차 자기-회귀 헌트로 확정. collectState가 committed를 `origin/HEAD(=origin/main)..HEAD` 커밋 수로 계산했으나, git-flow상 feature/fix는 develop에서 갈라져 develop로 PR(pr-create은 develop 우선 base 감지)이라, develop이 main보다 N커밋 앞서면 develop-기반 자기 커밋 0개 신규 브랜치도 origin/main..HEAD=N>0 → committed=true → decide()가 아직 아무 작업 안 한 브랜치에 feature-merge를 상시 오라우팅. 순수함수 `committedBaseRef(hasOriginDevelop, originHead)` 신설(develop 있으면 origin/develop, 없으면 origin/HEAD/폴백)로 pr-create과 정합하게 교정 — #181 isSolo가 prBase로 교정된 것과 동류(당시 committed는 미교정 잔여). route-intent-test 30→33. 주입형 조언이라 보호 약화 아닌 Low(실행은 게이트가 판단). 전체 스위트 회귀 0. GitHub #209. | 2026-07-07 | plugins/harness-guard/scripts/route-intent.mjs | tests/route-intent-test.sh, plugin.json(0.29.13) |

| **check-repo-sync 주석 스트립을 따옴표-인식으로 교정(High 회귀) + gradlew check 인식**(v0.29.14): 전 파일 전수 검증(Phase 2 계약 매트릭스)으로 확정. (A, High, 내 #205 회귀) stripComments가 #183(전체+인라인 # 전부 제거)→#205(전체-라인만 제거)로 두 번 다 coarse했다 — #205 좁힘 탓에 제거된 게이트의 sentinel이 활성 라인 **트레일링 주석**(`# TODO re-add gitleaks`)에만 남아도 존재로 오인(false OK, 드리프트 미탐, OLD=0 실측). 따옴표-인식 char-walk로 교정 — 진짜 YAML 주석(따옴표 밖 + 라인시작/공백뒤 #)만 제거, 따옴표 안 #(echo "issue #12 allow-test-removal")는 보존. → 전체-라인 주석 게이트=MISSING(#183)·트레일링 주석 게이트=MISSING(A)·따옴표 안 sentinel=보존(#205) 세 케이스 동시 만족(전부 실측). (C, Med) ci-gate 내용신호 test측에 `gradlew..check` 추가(gradlew check가 test를 transitive 실행). 픽스처 bad-sentinel-trailing-comment·good-gradlew-check 추가, repo-sync-test 8→10. 전체 스위트 회귀 0. GitHub #213. | 2026-07-07 | plugins/harness-guard/scripts/check-repo-sync.mjs | tests/repo-sync-test.sh, tests/fixtures/repo-sync/{bad-sentinel-trailing-comment,good-gradlew-check}, plugin.json(0.29.14) |

| **check-migration-safety 프로파일 적용성을 safe-default로 전환(High)**(v0.29.15): 전 파일 전수 검증(Phase 2)으로 확정. isProdApplicable(#197/#205)이 하드코딩 NONPROD_TOKEN_RE(test|dev|ci|local|it|e2e|integration)만 열거해 **staging/stage/uat/qa/sandbox/perf/demo/preview/acceptance** 등 미등록 비운영 프로파일을 default(운영 적용)로 오판 → 그 문서의 out-of-order:true가 운영(prod, ooo:false)을 덮어 게이트 통과(운영 부팅실패 클래스 놓침, 9개 프로파일 전부 OLD=0 실측). 근본원인=토큰 리스트는 본질적으로 불완전. **safe-default로 전환**: on-profile을 가진 문서는 (a) prod/production 토큰 포함 또는 (b) 비운영 프로파일의 부정(!test 등)일 때만 운영 적용(true), 그 외 명명된 프로파일은 미적용(false) — 리스트 무관. 9개 미등록 프로파일 전부 NEW=1(FAIL)로 교정, 기존 픽스처(test·!prod·test|prod·!test·default·prod-alias production-eu) 전부 보존 실측. 픽스처 bad-staging-profile 추가, migration-safety-test 16→18. 밴드-휴리스틱 3건(discovery greedy·100단위밴드·8자리 타임스탬프)도 확정됐으나 임계값 변경이 오탐 유발 위험이라 별도 신중검토(이번 안전배치 제외, 정직 보고). 전체 스위트 회귀 0. GitHub #214. | 2026-07-07 | scripts/check-migration-safety.mjs | tests/migration-safety-test.sh, tests/fixtures/migration-safety/bad-staging-profile, plugin.json(0.29.15) |

| **new-repo 권한병합 실패를 exit에 반영(Med) + pr-create self-PR 거절(Low)**(v0.29.16): 전 파일 전수 검증(Phase 2)으로 확정. (D) new-repo.sh가 branch protection 실패는 PROT_FAILED로 exit 1에 반영하나, 스택 권한 병합(merge-permissions) 실패는 ❌ 경고만 하고 '기계 셋업 완료' + exit 0 → 부분 프로비저닝(베이스라인만)을 성공으로 은폐(node 부재/강제실패 둘 다 exit 0 실측). SETUP_FAILED 플래그를 else에 설정하고 검증된 prot_exit_ok 게이트로 exit에 반영(PROT_FAILED와 대칭). (E) pr-create.sh의 is_base_branch가 main|develop만 하드코딩해 기본 브랜치명이 trunk 등이면 self-PR(trunk→trunk) 미차단(gh head==base 백스톱이나 push 후) → BASE 확정 후 BRANCH==BASE면 거절 추가. 구문·무회귀 확인(둘 다 gh/git impure 흐름이라 전용 단위테스트 대신 검증된 게이트 재사용+검사로 확인). 전체 스위트 회귀 0. GitHub #215. | 2026-07-07 | scripts/new-repo.sh, plugins/harness-guard/scripts/pr-create.sh | plugin.json(0.29.16) |

| **가드/게이트 판정 철학 — 파싱 안 된 문자열에서 의미론 추론 금지 + advisory 우회-군비경쟁 동결**(설계원칙, 버전 무관): 다각도 검토(7렌즈)로 이번 세션 반복 회귀(guard force-push 3회 재작성·migration 밴드 9회 패치)의 근본원인 확정 — (1) 정규식으로 셸 명령을·매직상수로 파일명 규약을 판정하는 것은 원리적으로 완결 상한이 없다(셸은 정규언어 아님; 밴드성은 표본 아닌 팀 규약). (2) 진짜 엔진은 **오배분된 위협모델**: guard.sh가 스스로 '보조 장치, load-bearing은 계층0(branch protection+CI)'이라 선언하면서도 verify-by-bypass 적대rigor를 서버-백스톱 있는 넛지에 적용 → 결정된 우회는 항상 존재하므로 종료조건 없는 arms race(force 과탐↔우회 재봉쇄 왕복 4회가 증거). **결정**: ① 위협모델 명시 — 상대 = 혼란한 에이전트지 공격자 아님. ② guard 검사 2분류: (a) 서버-백스톱 있는 git-flow 넛지(main/develop 커밋·force-push·gh) = 정규식 **동결**·우회테스트 제외·under-block 편향(과차단 안 함), (b) 로컬-only 파괴가드(reset --hard·rm -rf 코어·검증기삭제·npm -g) = 백스톱 없는 이 계층의 **진짜 load-bearing**, 흔한 형태만 하드블록. ③ 오탐 정책 = **비가역성×백스톱**(원칙#8): 로컬 비가역=하드블록, 서버-가역=넛지. ④ 판정기는 파싱 안 된 문자열에서 의미론을 무한 추론하지 않는다 — 규약은 선언에서 읽고, 명령은 실제 파서(shlex)로 토큰화. 정밀 판정에 정규식 epicycle 금지. → **재작성 압력을 끊는 계약**. 후속 재설계(guard shlex 통합·migration 선언기반)는 이 원칙 하에서 #220 로드맵 Phase 2. GitHub #220. | 2026-07-07 | plugins/harness-guard/scripts/guard.sh(헤더), docs/decisions.md | scripts/check-migration-safety.mjs, docs/intro.html, GitHub #219·#220 |

| **check-migration-safety 복합 프로파일 false-pass 교정 — #214 "닫았다" 주장 반증 후 재수정(High)**(v0.29.17): **2회차 다각도 재검토(직전 세션 작업을 적대검증)로 #214의 decision-log 주장이 반증됨** — safe-default(#214)의 부정 규칙 `/![\s"']*[a-z]/→true`가 `&`/`|` 구조를 무시하고 아무 `!`에나 발동해, **복합식** `on-profile: "staging & !test"`(양의 비운영항 staging + 부정 !test)·`test & !foo`·`qa,!smoke`가 여전히 운영 적용으로 오판 → ooo:true 크레딧 → 운영(prod ooo:false) false-pass(3종 exit=0 직접 재현). #214의 '거짓 프로파일 클래스를 닫았다'는 **좁힌 것에 불과**했다(복합식 누수). 교정: 부정 규칙을 **항 단위 분해**로 대체 — 표현식의 모든 항이 부정(!X)일 때만 적용(부정은 배제만), 양의 비운영 항이 하나라도 있으면 미적용. 복합/순수비운영/순수부정/양의prod 전수 재현으로 확정(staging&!test·test&!foo·qa,!smoke→FAIL, !test·test|prod·production-eu→정상), 기존 6픽스처 보존. 픽스처 bad-compound-profile 추가, migration-safety-test 18→19. **교훈: '고쳤다'는 자기 주장을 재검증 없이 신뢰하면 안 된다(#220 원칙#6).** 전체 스위트 회귀 0. GitHub #214(재개). | 2026-07-07 | scripts/check-migration-safety.mjs | tests/migration-safety-test.sh, tests/fixtures/migration-safety/bad-compound-profile, plugin.json(0.29.17) |

| **force-push 목적지 종단에 `)` 추가(서브셸 우회) + guard-matrix 과장 주장 정정**(v0.29.18): 2회차 재검토로 확정 — `(git push --force origin main)`(서브셸)가 exit=0 우회(직접 재현). 목적지 매처 종단 `([[:space:]]|$)`가 `)`를 미포함해 `main)`이 목적지로 안 잡히고, HAS_REF가 `main)`을 명시 refspec으로 오인해 현재-브랜치 판정도 스킵. 형제 가드(commit·F5·gh 전부 `)` 처리)와의 비대칭 오버사이트 → 종단에 `[)]` 추가로 정합(형제와 일관, #220 넛지 원칙과 별개인 오버사이트 교정). 동시에 guard-matrix-test의 'main/develop 도달 **모든** 형태/wrapper 커버' 주장이 서브셸 미포함으로 **거짓**이었음 — 'best-effort 넛지, 정본은 계층0'으로 정정하고 서브셸 케이스 3건 추가. 55→58 케이스, matrix.py 57 계약·guard-test 127·전체 스위트 회귀 0, `(...main)`→차단·feature/featmain 과탐 없음 실측. GitHub #220. | 2026-07-07 | plugins/harness-guard/scripts/guard.sh | tests/guard-matrix-test.sh, plugin.json(0.29.18) |

| **판정 철학 정정(2회차 재검토) — 동결 근거·분류 정확화**(문서, 버전 무관): 2회차 다각도 재검토가 Phase 0 판정철학 문서 자체의 4개 부정확성을 지적 → 교정. (1) category-(a) 동결의 근거를 '계층0 중복'에서 **위협모델(혼란한 에이전트)**로 이전 — 중복 논거는 protection 미설정(Private Free-plan)·드리프트 repo에서 성립 안 하고 그땐 guard가 유일선이라, repo-sync의 protection-on 점검이 그 공백을 메운다고 명시. (2) **F5(스펙-먼저)를 3번째 분류(서버 백스톱 없는 프로세스 넛지)**로 추가 — 비파괴이나 CI가 스펙을 강제 안 함(사람 리뷰만), (a)의 서버백스톱도 (b)의 비가역도 아님. guard.sh:156의 'F5 최종강제=계층0(리뷰/CI)' 부정확도 교정. (3) gh pr merge 서버 백스톱은 **CI-green만 커버, 리뷰-게이트 절차는 아님**(부분 백스톱) 명시. (4) python3 부재 fail-closed는 (b) load-bearing 계층 때문(파싱된 COMMAND 없이 판정 불가)이고 under-block 편향은 (a)에만 적용 — 정합. troubleshooting.md 넛지 박스도 npm -g 추가·동일 정정. **교훈 재확인: 방금 쓴 문서도 재검토하면 구멍이 있다.** 미해결(#220 Phase 2): 동결이 '이빨 없음'(guard-matrix가 여전히 (a) 적대테스트) — 재설계에서 정합. | 2026-07-07 | plugins/harness-guard/scripts/guard.sh(헤더), docs/troubleshooting.md, docs/decisions.md | GitHub #220 |

| **migration 괄호부정 false-pass 부분교정 + #224(서브셸 종단) 되돌림**(v0.29.19): **3회차 재검토가 직전 두 수정(#223·#224)의 새 결함을 또 잡음**(내 '고쳤다' 주장 3연속 반증). (1) #223 항-분해가 **괄호식** 미처리 — `on-profile: "!(prod | staging)"`에서 규칙2 `\bprod\b`가 부정 안 bare prod에 매칭해 항-분해 전 true 단락 → false-pass(운영 부팅실패 놓침, 3종 exit=0 직접 재현). 괄호(`[()]`) 있으면 **safe-default 미적용(false)**로 부분 교정(드문 over-block `!(test)`=FAIL 감수, 위험한 false-pass 차단 — 비가역>가역). **주의: 부분 교정 — '클래스를 닫았다' 주장 안 함.** 완전 해법은 boolean 파서(#220-A). (2) #224(force-push 종단 `)` 추가)는 애초에 #220 '동결' 원칙을 내가 어긴 수정이었고 `echo "…main)"` mention을 오차단하는 **over-block 회귀**를 냈다(category(a)는 under-block 편향인데 위반) → **되돌림**. 서브셸 우회는 서버-백스톱 있는 수용된 best-effort 갭으로 문서화, matrix에 echo-mention ALLOW 회귀방지 케이스 추가. 픽스처 bad-paren-neg-profile 추가, migration-test 19→20, echo-mention·55-matrix 무회귀 실측. **교훈(강조): isProdApplicable 정규식 패칭이 정확히 #220 원칙이 경고한 whack-a-mole — 3연속 실패. 더 패치 말고 #220-A 파서 또는 게이트 warn 강등으로 가야 한다.** 전체 스위트 회귀 0. GitHub #219·#220. | 2026-07-07 | scripts/check-migration-safety.mjs, plugins/harness-guard/scripts/guard.sh | tests/migration-safety-test.sh, tests/guard-matrix-test.sh, tests/fixtures/migration-safety/bad-paren-neg-profile, plugin.json(0.29.19) |

(시점 2026-06은 하네스 구축 시 일괄 소급 기재 — 이후 결정부터 개별 날짜로 기록)
