<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!-- 공백·들여쓰기·줄바꿈은 google-java-format(Spotless)이 소유한다 — 포매터가 강제하므로
     checkstyle에는 공백 규칙을 두지 않는다(중복·충돌 방지). checkstyle은 포매터가 다루지 않는
     '의미 규칙'만 본다: UnusedImports·NeedBraces·EmptyBlock·MagicNumber.
     포맷 자동수정: ./gradlew spotlessApply  ·  CI 강제: ./gradlew check (spotlessCheck 포함). -->
<module name="Checker">
  <property name="charset" value="UTF-8"/>
  <property name="severity" value="error"/>

  <module name="SuppressionSingleFilter">
    <property name="files" value=".*Test\.java$"/>
    <property name="checks" value="MagicNumber"/>
  </module>

  <module name="TreeWalker">
    <module name="UnusedImports"/>
    <module name="NeedBraces"/>
    <module name="EmptyBlock">
      <property name="option" value="text"/>
    </module>
    <module name="MagicNumber">
      <property name="ignoreNumbers" value="-1, 0, 1, 2"/>
      <property name="ignoreAnnotation" value="true"/>
    </module>
  </module>
</module>
