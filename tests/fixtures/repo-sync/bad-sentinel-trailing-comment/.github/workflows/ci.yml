name: ci-gate
on: [push]
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - run: npm run lint
      - run: npm test
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: wagoid/commitlint-github-action@v6
        with:
          configFile: ./commitlint.config.cjs
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo hi   # TODO re-add allow-test-removal label check
      - run: echo hi   # TODO re-add gitleaks/gitleaks-action@v2 step
